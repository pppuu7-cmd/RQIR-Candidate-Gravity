# RQIRCG research ledger addendum — RSC-MAPF1

## Authority

Starting main: `70b180b237057b182a00c7cca11a95383cf7c775`.

Preregistration: `2e5b573a5a8fca461df73fbfcd59b9633fe76b52` — `prereg/RCG002_RSC_MAPF1_MINIMAL_MULTI_AXIOM_PACKAGE_FORMATION_PREOUTCOME.md`.

Terminal result: `8641e1e06fa2d122f2e726f44a13cb3f05f5b486` — `results/RCG002_RSC_MAPF1_MINIMAL_MULTI_AXIOM_PACKAGE_FORMATION_TERMINAL.md`.

No GitHub Actions production was required. No connected RCG-002 observable was computed.

## Classification

`RSC_PACKAGE_SCHEMA_ONLY_MODEL_DEFINITION_INCOMPLETE`.

Secondary:

`CRVQ_FORMAL_CLOSURE_DOES_NOT_FIX_INDEPENDENT_SOURCE_AND_STATE_DATA_SCOPED`.

RSC remains `NEAR_SURVIVOR_NOT_SELECTED`.

No new RCG-002 version is authorized.

## Frozen package

MAPF1 assembled the Closed Relational Variational-Quantum (CRVQ) skeleton from current principle-level commitments:

- closed realization completeness;
- unified variational source/self-source;
- minimal local covariance and exact linearized recovery;
- complete quantum preparation constitution;
- derived normalized positive operational influence;
- exact G97/weak-field recovery;
- independent-construction firewall.

PASS required explicit population of source ontology/action, curvature-coupling rule, carrier self-source/constraints, quantum state/measure/boundary rule and operational reduction before outcomes.

## Exact package audit

Mandatory slots S1-S10 were not all populated.

Main unresolved object classes:

1. local source/action physical representation for probe+apparatus/support;
2. rule fixing dimensionless curvature/nonminimal couplings;
3. nonlinear carrier action/self-source and constraint propagation;
4. physical unobserved quantum state/measure/boundary prescription;
5. exact reduced operational map in the same nonlinear realization.

G97 and the RCG-002 seed supply operational/collective objects, not these local microphysical objects.

## New source calibration fact

The preregistered family

`S_xi[g,phi]=Integral sqrt(-g)[-1/2 (partial phi)^2 - V(phi) - (xi/2)R phi^2]`

retains local covariance, a variational source, identical exact flat matter equations for all `xi`, two-derivative calibration equations and no new dimensional scale from `xi`, while its flat-background Hilbert stresses differ by the conserved improvement

`Delta T_mn = Delta xi (eta_mn Box - partial_m partial_n) phi^2`.

Therefore package-level words `one action`, `covariant`, `two derivative`, `no new scale` and `minimal` do not fix the local source without a physical field/basis rule.

This is calibration only, not RCG-002 matter physics.

## New quantum lower-domain fact

Freeze a history selector `q_x` that vanishes on every authorized lower-order history domain and fixed microscopic dynamics

`U_x=exp(i alpha q_x Z)`

with nonzero frozen `alpha`.

For `rho_0=|0><0|`:

`K_0(x,y)=exp(i alpha[q_x-q_y])`.

For `rho_plus=|+><+|`:

`K_plus(x,y)=cos(alpha[q_x-q_y])`.

Both are normalized PSD kernels generated by the same unitary dilation with valid pure states. On every lower-order domain, `q_x=q_y=0`, so both give exactly `K=1`; outside that domain they can differ.

Thus closed + unitary + pure + positive + exact lower-order recovery does not fix the physical unobserved state or higher reduced map.

No connected RCG-002 observable was evaluated.

## Combined structural fact

Source and state calibrations occupy logically independent model-definition slots and can be combined as a Cartesian-product synthetic family. This shows that bundling formal closure principles does not collapse one ambiguity into the other.

It does **not** establish a two-dimensional physical gravitational completion space. Physical completion space remains `UNDEFINED`; selector rank remains `UNDEFINED_PHYSICAL_MAP_MISSING`.

## Programme state

- current version: `RCG002_CURRENT_VERSION_NONLINEAR_DYNAMICS_UNDERDETERMINED`;
- RSC: `NEAR_SURVIVOR_NOT_SELECTED`;
- readiness: 66%;
- theory established: 0%;
- `chi_ABC`: unauthorized.

## Next gate

`RSC_NEW_AXIOM_ANCHOR_BUDGET_PREOUTCOME_GATE`.

Before proposing a concrete rescue model, prospectively audit each missing content axiom against an independent anchor. At minimum distinguish:

- source ontology/action anchor;
- curvature/nonminimal-coupling anchor;
- carrier self-source/constraint anchor;
- quantum state/measure/boundary anchor;
- operational reduction/causal anchor.

A proposed axiom must be anchored to an existing RQIR-CG requirement, an exact symmetry/consistency obstruction, or an external physical datum before connected outcomes. `Simplest`, `standard`, `vacuum`, `minimal`, `natural`, resemblance to GR/QFT, or blocker-removal convenience is not enough.
