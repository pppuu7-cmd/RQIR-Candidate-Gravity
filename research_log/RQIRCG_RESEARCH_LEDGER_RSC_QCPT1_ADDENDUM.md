# RQIRCG clean ledger addendum — RSC-QCPT1

## Gate

`RSC_QUANTUM_CONSTITUTION_PRINCIPLE_TRIAGE_PREOUTCOME_GATE`

Starting authority: `681008eb19b3ceb105b63a289dbeb630698b2c75`.

Preregistration: `4111ab9504ed77398c3a947f276c254132bfc1ce`.

Terminal result: `7256a885c05c470079a616d18de7f8750280ab37`, `results/RCG002_RSC_QCPT1_QUANTUM_CONSTITUTION_TRIAGE_TERMINAL.md`.

## Result

`NO_PREOUTCOME_QUANTUM_CONSTITUTION_SELECTED`.

Secondary:

`RSC_QUANTUM_SIDE_REQUIRES_NEW_STATE_MEASURE_CONSTITUTION_SCOPED`.

Combined with SCPT1:

`RSC_CURRENT_PRINCIPLE_PACKAGE_REQUIRES_MULTIPLE_NEW_MODEL_DEFINING_AXIOMS_SCOPED`.

RSC remains `NEAR_SURVIVOR_NOT_SELECTED`.

## Frozen proposal verdicts

- UCQ — `REJECT_MISSING_ACTION_STATE_MEASURE_AND_CTP_CONSTITUTION`.
- PCF — `REJECT_RANK_ONE_FORM_DOES_NOT_FIX_NONLINEAR_PHASE_FUNCTIONAL`.
- GQC — `REJECT_LINEAR_GAUSSIAN_BASELINE_NOT_NONLINEAR_RSC_CONSTITUTION`.
- MPE — `REJECT_MINIMUM_NOISE_LEAVES_COHERENT_EXTENSION_UNFIXED`.

Mandatory survivor count: 0.

## Exact new structural certificate

For fixed controlled microscopic dynamics

`U=sum_x |x><x| tensor U_x`,

the reduced influence multiplier for initial unobserved state `rho_E` is

`K_rho(x,y)=Tr[rho_E U_y^dagger U_x]`.

Every density operator gives a unit-diagonal PSD kernel. The same `U_x` can give different kernels for different `rho_E`.

Explicit control:

`U_0=I`, `U_1=Z`.

- `rho_E=|0><0|` gives `[[1,1],[1,1]]`;
- `rho_E=|+><+|` gives `[[1,0],[0,1]]`.

Thus fixed microscopic unitary dynamics does not uniquely determine the operational influence map without a physical state/measure rule.

## Retained controls

RSC1 remains authoritative: deterministic coherent phase data admit a continuum of normalized positive noise/decoherence completions.

NP1 remains authoritative: normalized positive extensions preserving all frozen lower-order faces include a continuum of coherent/incoherent directions. In particular `gamma=0` removes added decoherence while leaving coherent `lambda` freedom, so minimum-noise selection alone is insufficient.

## Claim ceiling

No connected `chi_ABC` or connected noise observable was computed. No physical completion-space dimension is inferred. No universal nonuniqueness theorem, no failure of all quantizations, no new-physics or full-QG claim.

Readiness remains 66%. Theory established remains 0%. Physical selector rank remains `UNDEFINED_PHYSICAL_MAP_MISSING`. Physical nonlinear completion space remains `UNDEFINED`.

## Next gate

`RSC_MINIMAL_MULTI_AXIOM_PACKAGE_FORMATION_PREOUTCOME_GATE`.

Any future RSC descendant must prospectively bind both independently missing layers in one realization: a microphysical source/action constitution and a physical quantum state/measure/influence constitution, together with carrier self-source, constraints/causality, positivity and exact weak-field recovery. No connected outcome may be used to choose the package.
