# RQIRCG clean research ledger — RSC AB1 addendum

Gate: `RSC_NEW_AXIOM_ANCHOR_BUDGET_PREOUTCOME_GATE`.

Preregistration: `80962231e55adbf96d7d24f47aeb9cd382d2e0a6`.

Terminal result commit: `a188e4bae2f46c4ddfc7da86f663c80b0b82cc79`.

Classification:

`RSC_NO_NEW_MODEL_DEFINING_AXIOM_INDEPENDENTLY_ANCHORED_PREOUTCOME_SCOPED`.

Secondary:

`STRONG_STRUCTURAL_ANCHORS_EXIST_BUT_NO_LEVELIII_MODEL_SELECTOR_SCOPED`.

## Frozen result

Five strongest content-axiom candidates were audited under Type-R / Type-O / Type-E authority and Constructor -> Falsifier -> Judge passes:

- A source completeness `CSRC`: budget 2, `PARTIALLY_ANCHORED`;
- B empirical WEP constraint `EWU`: budget 2, `PARTIALLY_ANCHORED`;
- C two-derivative massless-spin-2 gauge deformation `CGD2`: budget 2, `PARTIALLY_ANCHORED`; conditional Level III only inside extra action/off-shell or scattering theorem premises not selected by current RQIRCG;
- D Hadamard state admissibility `HSA`: budget 2, `PARTIALLY_ANCHORED`;
- E causal CPTP operational reduction `CCR`: budget 2, `PARTIALLY_ANCHORED`.

No candidate reached budget 3 / Level III. No new version content was selected.

## New scientific fact

Current RQIRCG is not devoid of physical constraints on the missing RSC slots. Instead there is a **selection-power gap**: independently anchored consistency/experimental conditions constrain admissible classes but each route to a definite model slot still requires an additional unanchored model datum.

The strongest near-selector is nonlinear massless-spin-2 consistency. External action-deformation and on-shell S-matrix theorems strongly constrain spin-2 interactions inside their premise classes, but current RQIRCG does not independently select those full premise classes. The theorem cannot serve as authority for its own applicability.

## Status retained

- RSC: `NEAR_SURVIVOR_NOT_SELECTED`.
- frozen version: `RCG002_CURRENT_VERSION_NONLINEAR_DYNAMICS_UNDERDETERMINED`.
- programme readiness: 66%.
- theory established: 0%.
- physical selector rank: `UNDEFINED_PHYSICAL_MAP_MISSING`.
- physical nonlinear completion space: `UNDEFINED`.
- `chi_ABC`: unauthorized and not computed.

## Claim ceiling

No universal no-go, no GR import, no unique state/source, no physical completion-space dimension, no connected prediction, no new-physics or full-QG claim.

## Exact next gate

`RSC_SPIN2_CONSISTENCY_PREMISE_AUTHORITY_PREOUTCOME_GATE`.

This gate may test only whether one of the premise frameworks behind the strongest spin-2 consistency routes is independently forced by RQIRCG. It may not adopt Einstein dynamics because it is familiar or because it closes the blocker.

Recommendation: `PRO_VALUE_NEXT_GATE=VERY_HIGH` because the remaining issue is a foundational comparison of inequivalent premise classes, not a straightforward computation.
