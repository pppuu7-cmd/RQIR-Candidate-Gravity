# Iteration 001 — independent RQIR-derived candidate construction

**Date:** 2026-09-11  
**Branch:** `rqir-derived-model-v0`

## Objective

Begin a gravity model from frozen RQIR v1.0 itself, without using the architecture/equations of the separately developed polygon-derived QGR candidate.

## Authority imported

Pinned the source RQIR repository and restricted construction inputs to frozen model-independent methodology and model-boot templates.

## Result A — root/minimal branch

Instantiated `ANSATZ-RQIR-MIN`.

- state space: perturbative quantum matter x metric gravity physical sector;
- single covariant low-energy action;
- CTP parent object;
- common derivation path for `J,N,D,chiR`;
- first weak-field derivation completed;
- Newtonian normalization passed: `nabla^2 Phi = 4 pi G rho`;
- gravity-off structure identified;
- provisional conclusion: under metric-only/local/minimum-complexity assumptions the branch lies in the C5 perturbative quantum-GR/EFT class.

This is retained as a null/root result, not advertised as a new theory.

## Result B — RQIR-native channel branch

Instantiated `ANSATZ-RQIR-QLC` (Quantum-Limited Relational Gravity Channel).

Core hypothesis:

- GR retarded response fixes the channel transfer `K`;
- complete positivity constrains added noise;
- no excess interface noise is introduced beyond the CP minimum in each uniquely canonicalizable physical mode block.

One-mode canonical calculation gives

`y_min = hbar |1-eta| / 2`

for `K=sqrt(eta) I`, and more generally

`y_min_phys = hbar |omega_Y - k^2 omega_X| / 2`

for unequal physical commutator scales.

This is the first quantitative QLC closure relation.

## Current scientific interpretation

RQIR by itself does not justify arbitrary extra fields/functions. The independent route first reproduces a C5-like root. The QLC branch is a more RQIR-native operational candidate, but its novelty remains unestablished until the physical GR mode map and C5 equivalence audit are completed.

## Next derivation

1. build a conserved source + relational output toy mode from weak-field GR;
2. map its physical `K, Omega_X, Omega_Y` into the QLC CP floor;
3. verify physical `G -> 0` scaling;
4. compare QLC against a pure-environment Stinespring realization / C5 quantum-gravity channel;
5. if QLC is exactly C5-degenerate, retain it as an operational representation and search for the smallest RQIR-justified relaxation rather than adding arbitrary novelty.
