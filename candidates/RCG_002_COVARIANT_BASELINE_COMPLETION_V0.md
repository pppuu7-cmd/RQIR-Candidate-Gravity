# RCG-002 covariant baseline completion v0

Status: **HYPOTHESIS / BASELINE EMBEDDING ONLY — NOT A VALIDATED THEORY AND NOT A NOVELTY CLAIM**

Date: 2026-09-13

## Why this object exists

G57-P found that the operational RCG-002 seed has an explicit two-probe channel and a validated weak-field source/energy-to-phase chain, but no candidate-owned spacetime gravitational field variable, dynamical principle, spacetime covariance law, conservation/Bianchi structure, or relativistic retarded dynamics. Before searching for a genuinely new deformation, the programme needs a minimal covariant baseline that can reproduce the already validated weak-field branch without silently declaring standard physics to be new physics.

This document therefore defines the **smallest standard linearized spin-2 embedding** of the current RCG-002 weak-field branch. If the existing RCG-002 phase is fully reproduced by this baseline, the correct conclusion is *baseline equivalence at that order*, not new physics. Any future novelty must then enter through a separately stated deformation or a deeper nonlinear/relational/quantum structure.

Nothing here changes the historical seed file or its chronology. Nothing here upgrades programme readiness by itself.

## Domain and field variable

Work on a nearly Minkowski spacetime with signature `(-,+,+,+)`:

`g_{mu nu} = eta_{mu nu} + h_{mu nu}`, with `|h_{mu nu}| << 1`.

Define the trace reverse

`bar h_{mu nu} = h_{mu nu} - (1/2) eta_{mu nu} h`,

where `h = eta^{mu nu} h_{mu nu}`.

At this stage `h_{mu nu}` is only a **linearized gravitational field variable**. No nonlinear metric completion is asserted.

## Linearized coordinate/gauge law

Under an infinitesimal coordinate shift generated by `xi_mu`,

`h_{mu nu} -> h_{mu nu} + partial_mu xi_nu + partial_nu xi_mu`.

Equivalently the trace-reversed field transforms by the corresponding linearized rule. We use the de Donder/Lorenz condition

`partial^mu bar h_{mu nu} = 0`.

This is a linearized gauge law only; it is not yet a full nonlinear diffeomorphism constitution.

## Source and conservation rule

The baseline source is a symmetric stress-energy object `T_{mu nu}` obeying

`partial_mu T^{mu nu} = 0`

on the background spacetime. For quantum matter the same coupling may be written with the stress-energy operator `hat T_{mu nu}`; this document does not yet specify a complete interacting quantum-gravity measure or nonlinear renormalized stress tensor.

The conservation condition is not optional in this baseline: it is the compatibility condition that removes gauge-dependent longitudinal source contributions.

## Candidate-owned linearized dynamical rule

Freeze the baseline equation

`Box bar h_{mu nu} = -(16*pi*G/c^4) T_{mu nu}`.

With a retarded Green function `D_ret` satisfying the chosen wave-operator convention, the sourced retarded solution is

`bar h_{mu nu}(x) = bar h^free_{mu nu}(x) + (16*pi*G/c^4) Integral d^4x' D_ret(x-x') T_{mu nu}(x')`,

with the overall sign understood consistently with the wave-operator/Green-function convention used in a given calculation.

The divergence of the field equation vanishes for a conserved source, so the de Donder constraint is dynamically compatible at this linearized level. This is the candidate's first explicit field-dynamics hypothesis; it remains unvalidated until the dedicated gate closes.

## Static nonrelativistic reduction

For a slowly moving source, `T_00 ~= rho c^2`. Define the Newtonian potential by

`Phi = -(c^2/4) bar h_00`.

The static field equation then reduces to

`nabla^2 Phi = 4*pi*G rho`.

For a normalized isotropic Gaussian source of width `s`,

`rho_s(r) = (2*pi*s^2)^(-3/2) exp[-r^2/(2s^2)]`,

this gives

`Phi_s(r) = -G M erf[r/(sqrt(2)s)]/r`.

Therefore the finite-size kernel already validated by G53-W/G56-F2 is exactly the static Gaussian weak-field limit that this baseline must reproduce.

## Two-source interaction and RCG-002 branch phase

For two nonrelativistic source branches A and B, the baseline static interaction energy is

`V_AB = -G Integral d^3x d^3y rho_A(x) rho_B(y) / |x-y|`.

For branch labels `a,b in {0,1}`, write the branch phase

`phi_ab = -(1/hbar) Integral dt V_ab(t)`.

The operational controlled-phase invariant is the quotient

`chi = phi_11 - phi_10 - phi_01 + phi_00`.

This is the quantity that must reduce to the already validated G52-H/G53-W/G54-Q operational chain. Local one-body phases cancel from this quotient.

## Linearized quantum baseline and novelty lock

For the baseline-equivalence audit only, the free linearized spin-2 field may be canonically quantized and coupled linearly to a conserved quantum stress-energy source. Integrating a Gaussian free field produces the familiar quadratic source functional built from spin-2 Green functions. This statement is used only to define a **standard linearized QFT baseline** for comparison.

No claim is made here that this baseline is new, complete, UV finite, nonlinear, or a full quantum gravity theory. No nonlinear gravitational path-integral measure, gauge-fixed interacting measure, ghost determinant, renormalization prescription, or nonperturbative Hilbert space is supplied by this v0 document.

### Novelty lock

If G58 shows that the current RCG-002 weak-field phase/channel is exactly reproduced by this standard linearized baseline, then the current weak-field evidence is **not evidence for new physics**. A future RCG-002 theory must introduce an independently motivated extra structure `Delta Gamma` (or equivalent) that:

1. is not removable by a local phase/basis change;
2. preserves the required conservation/gauge consistency;
3. has a controlled retarded/covariant interpretation;
4. reduces to the validated weak-field branch where required;
5. yields at least one discriminator not reproduced by the standard baseline or the calibrated classical comparator families.

No form for `Delta Gamma` is chosen in this document.

## Limits and unresolved items

- `G -> 0`: the sourced interaction term vanishes in the baseline equation; this still requires a dedicated frozen test before being recorded as a derived result.
- `hbar -> 0`: **unresolved**. A phase `S/hbar` does not have a naive pointwise classical limit; a stationary-phase/WKB or observable-level statement is required.
- full nonlinear diffeomorphism invariance: **unresolved**.
- nonlinear Bianchi/constraint structure: **unresolved**.
- QFT/EFT renormalized baseline beyond the free linearized comparison: **unresolved**.
- full gravitational quantization/measure: **unresolved**.

## Scope ceiling

This v0 object is a linearized, weak-field, nearly Minkowski **baseline completion hypothesis** designed to locate the novelty boundary of RCG-002. It is not full GR, not a generally covariant nonlinear theory, not established quantum gravity, and not a new-physics claim.