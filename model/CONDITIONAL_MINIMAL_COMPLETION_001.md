# Conditional Minimal Completion 001 — the least-structured local metric completion is low-energy quantum GR

**Status:** CONDITIONAL DERIVATION / CONTROL RESULT  
**Not a theorem from RQIR alone.** The conclusion follows only after adding the explicit minimality assumptions below.

## 1. Assumptions

In the controlled low-energy domain assume:

1. four-dimensional Lorentzian spacetime;
2. the metric is the only gravitational field at leading order;
3. local generally covariant dynamics;
4. no preferred background structure in the physical equations;
5. leading equations contain at most two derivatives of the metric;
6. universal leading matter coupling through the metric/stress tensor;
7. ordinary quantum probability structure;
8. higher-derivative corrections are organized as an EFT rather than promoted arbitrarily to the leading dynamics.

These are stronger assumptions than frozen RQIR itself and are therefore recorded explicitly.

## 2. Leading metric action

For a local diffeomorphism-invariant scalar density built only from the metric and containing at most two derivatives, the non-boundary leading structures are the volume term and Ricci scalar,

\[
S_g^{(2)}
=\int d^4x\sqrt{-g}\,(a+bR).
\]

Choosing the normalization by the Newtonian limit gives

\[
S_g^{(2)}
=\frac{2}{\kappa^2}\int d^4x\sqrt{-g}(R-2\Lambda),
\qquad
\kappa^2=32\pi G.
\]

Thus the root leading dynamics is Einstein-Hilbert gravity.

## 3. Higher-order low-energy completion

Once higher derivatives are admitted under EFT power counting, the action becomes

\[
S_{EFT}=S_{EH}+S_m+\int\sqrt{-g}\left(
 c_1R^2+c_2R_{\mu\nu}R^{\mu\nu}+c_3R_{\mu\nu\rho\sigma}R^{\mu\nu\rho\sigma}+\cdots
\right),
\]

modulo identities, boundary/topological combinations and field-redefinition redundancies appropriate to the chosen basis.

The Wilson coefficients are not determined by RQIR alone.

## 4. Quantum interface

Applying ordinary low-energy quantization to the physical perturbative gravitational sector gives the conventional low-energy quantum-GR/EFT class:

\[
\text{EH + matter + EFT} \quad\xrightarrow{\text{quantize}}\quad C5.
\]

Its CTP generating functional already supplies

\[
J,N,D,\chi^R,W^{(3)},W^{(4)},\ldots
\]

order by order from one coherent dynamics, subject to gauge/BRST, renormalization and EFT control.

## 5. RQIR gate compatibility

This completion is structurally capable of satisfying the frozen RQIR demands:

- Q1-Q7 all have well-defined controlled low-energy representations in principle;
- the Newtonian limit was explicitly checked in `ANSATZ-RQIR-MIN`;
- ordered response/noise arise from the same CTP dynamics;
- higher vertices arise from GR/matter interactions;
- gauge/conservation constraints are part of the underlying covariant structure;
- comparator C5 is the model itself, so there is no C5 novelty claim.

Actual detector-level PASS states still require the corresponding derivations and apparatus maps; structural compatibility is not a substitute for those certificates.

## 6. Conditional result

\[
\boxed{
\text{Frozen RQIR}
+\text{metric-only}
+\text{local covariance}
+\text{two-derivative leading dynamics}
+\text{ordinary quantization}
+\text{minimum complexity}
\Rightarrow
\text{low-energy quantum GR/EFT (C5-like)}
}
\]

The result explains why the independent funnel repeatedly falls back into C5 before any arbitrary deformation is added.

## 7. Scientific implication

If the objective is specifically a **new** theory rather than the least-structured theory compatible with RQIR, at least one assumption above must be relaxed or supplemented by an additional physical principle.

That relaxation cannot be claimed to have been derived from RQIR unless a frozen RQIR obstruction to the C5-like root is first demonstrated.

## 8. Current decision

No such obstruction has been demonstrated in the independent branch. Therefore the present RQIR-only reconstruction does **not** justify modifying the C5-like minimal completion.
