# RQIR-Derived Gravity-Interface Equivalence Class v0

**Status:** ACTIVE RECONSTRUCTION OUTPUT  
**Type:** equivalence class / admissible-model family, not a unique microscopic theory  
**Authority:** frozen RQIR v1.0 plus independent negative results `RQIR-CG-NG-001` and `RQIR-CG-NG-002`.

## 1. Purpose

This document records the strongest gravity-model statement that is currently justified by the frozen RQIR funnel **without adding an extra microscopic postulate**.

## 2. Class definition

Let `E_RQIR(D)` be the class of gravity–quantum interfaces in a declared domain `D` satisfying all applicable frozen RQIR requirements.

A member `M in E_RQIR(D)` must supply one coherent dynamics/generating object and derive from it

\[
\mathcal H_M=\{J,N,D,\chi^R,C^{(3)}_{\rm conn},C^{(4)}_{\rm conn},\ldots\},
\]

plus the relational detector/process map required for the claimed observables.

## 3. Mandatory structural conditions

Every member must satisfy, where applicable:

- gauge/relational observability;
- conservation/Bianchi/Ward consistency;
- causal retarded support and no operational superluminal signalling;
- positivity/unitarity/complete positivity appropriate to the representation;
- renormalization and smearing of composite stress-energy objects;
- controlled `hbar -> 0`, `G -> 0`, flat, weak-field/Newtonian and EFT limits;
- one-dynamics derivation of mean, fluctuation, ordered response and higher objects;
- explicit comparator audit C0-C6;
- detector-level calibration/nuisance identifiability before a distinction is claimed;
- physical resource/measurability closure for an experimental claim.

## 4. Root representative

The minimum local metric-only representative is `ANSATZ-RQIR-MIN`:

\[
S=S_{EH}+S_{matter}+S_{EFT}+S_{ct},
\]

with a perturbatively quantized gravitational mediator in the controlled low-energy domain.

Its Newtonian normalization has been checked explicitly and it is provisionally C5-like. It is therefore the natural null/root representative of `E_RQIR` rather than a new-theory claim.

## 5. Gaussian channel representative

`ANSATZ-RQIR-QLC` supplies an operational channel representation of a subset of `E_RQIR`.

Its CP-saturating one-mode law is mathematically consistent but is not gravity-specific: a conventional pure Gaussian unitary dilation reproduces it. Adding generic L3 retarded/ordered response also remains compatible with an ordinary quantum mediator.

Thus QLC currently represents part of the equivalence class rather than a distinct microscopic theory.

## 6. Residual freedom

After the frozen low-order constraints are imposed, the remaining microscopic freedom can be represented schematically by higher connected/response structures

\[
\{W^{(n)}\}_{n\ge3}
\]

and by the detailed state-space/vertex/process realization consistent with the frozen gates.

RQIR constrains these objects but does not presently provide a unique universal closure map

\[
\{J,N,D,\chi^R\}\mapsto\{W^{(3)},W^{(4)},\ldots\}.
\]

## 7. Operational equivalence relation

For models `M1,M2`, define

\[
M_1\sim_D M_2
\]

when, after the same baseline transformation and nuisance/calibration profiling, all RQIR observables accessible in domain `D` have indistinguishable predicted probability distributions at the justified theoretical/experimental resolution.

The physically inferred object is then the quotient

\[
\boxed{\mathcal E_{RQIR}(D)/\sim_D}
\]

rather than an arbitrarily selected microscopic representative.

## 8. What would shrink the class

The class shrinks only when one of the following is supplied:

1. a new RQIR observable sensitive to presently unconstrained higher structure;
2. a consistency theorem linking multiple CTP orders more strongly than the current gates;
3. a gravity-specific obstruction excluding otherwise valid C4/C5 dilations;
4. an independently motivated extra physical postulate with new falsifiable predictions.

Only items 1-3 could properly be called additional **RQIR-forced** restriction. Item 4 defines a new candidate hypothesis tested by RQIR.

## 9. Current reconstruction result

The current independent experiment therefore yields:

\[
\boxed{
\text{Frozen RQIR v1.0}
\Rightarrow
\text{a sharply constrained gravity-interface equivalence class}
}
\]

with a C5-like metric-quantum root and no established RQIR-only reason yet to select a new microscopic dynamics.

This is the authority state from which any next Candidate Gravity hypothesis must start.
