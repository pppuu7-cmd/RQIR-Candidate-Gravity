# Blind Comparison Checkpoint 001 — Frozen RQIR-only v0 vs KMQGB polygon authority

**Status:** META-COMPARISON COMPLETE / MODEL-TO-MODEL COMPARISON BLOCKED  
**RQIR side:** `INDEPENDENT_RQIR_RECONSTRUCTION_FREEZE_V0.md`  
**Polygon side:** `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`, `main`, current recovery/state authorities.  
**Rule:** no result below may be back-written into the frozen RQIR-only derivation as though independently derived there.

## 1. Why the full QGR comparison is not yet admissible

The current KMQGB authority states:

- repository/methodology readiness: 100%;
- Candidate Gravity scientific readiness: 24%;
- `promotable_ansatz=false`;
- `robust_unique_residual=false`;
- Paper IV global decision: `NOT_YET_AUTHORIZED`;
- Candidate Gravity activation condition: a terminal Paper-IV D7 decision `NEW_REQUIRED`.

The KMQGB README likewise states that no Candidate Gravity ansatz is promoted in the current authority state.

Therefore there is presently no frozen polygon-derived QGR model with authority equivalent to the frozen RQIR v0 result. An equation-by-equation candidate comparison would compare a frozen reconstruction against a moving/conditional synthesis search and would violate the anti-overfitting discipline.

## 2. Admissible comparison level

What can be compared now is the **logical endpoint / methodology-level residual** of the two independent routes.

The comparison questions are:

1. Do both routes independently find that known consistency constraints are insufficient to select a unique new theory?
2. Do both routes identify unresolved upstream freedom rather than a hard C5-excluding residual?
3. Does either route presently authorize `NEW_REQUIRED` / a C5-distinct model?
4. Is there any contradiction that forces reopening the frozen RQIR-only result?

## 3. Independent convergence A — no authorized new model

### Frozen RQIR-only route

Result:

`Frozen RQIR v1.0 -> constrained equivalence class`, with a C5-like low-energy quantum-GR/EFT witness and zero discovered theory-level RQIR/C5 conflicts.

`QG-007` remains blocked for a C5-distinct theory.

### KMQGB polygon route

Current authority:

- Candidate Gravity readiness remains 24%;
- no promotable ansatz;
- no robust unique residual;
- Paper IV remains `NOT_YET_AUTHORIZED`;
- `NEW_REQUIRED` is not authorized.

### Comparison

The two routes independently agree on the present decision:

\[
\boxed{\text{do not promote a new microscopic gravity theory yet.}}
\]

This convergence is nontrivial because the RQIR-only branch was frozen before inspecting the current polygon authority.

## 4. Independent convergence B — conditional generators do not solve upstream selection

KMQGB `SYNTHESIS_020_MUTUAL_MATTER_GRAVITY_CLOSURE_FIXED_POINT.md` tests mutual matter↔gravity closure and rejects it as a standalone parent because the reverse matter-selection law remains unspecified and lower-order matter functional freedom survives.

KMQGB `SYNTHESIS_021_MODULAR_FLOW_STATE_TO_DYNAMICS_PARENT.md` tests modular flow and finds a real conditional map

`(algebra, state) -> modular flow`,

but rejects it as a standalone parent because the algebra/state remain primitive unselected data.

This independently mirrors the frozen RQIR result:

- QLC gives a valid conditional channel construction but not a unique gravity microdynamics;
- Ward/positivity/causality constrain higher orders but leave residual freedom;
- a new closure law added merely to remove that freedom would be an extra hypothesis, not an RQIR consequence.

Common structural lesson:

\[
\boxed{\text{a powerful downstream map is not a unique parent law if its upstream data remain free.}}
\]

## 5. Independent convergence C — comparator discipline dominates novelty pressure

RQIR-only premise closure says that if C5 satisfies the same premises, consequences of those premises cannot exclude the whole C5 family.

KMQGB independently fail-closes Candidate Gravity promotion while known-framework completion/attribution and same-realization obligations remain unresolved; `BLOCKED` is not treated as exclusion evidence and `NEW_REQUIRED` is withheld.

Thus both routes implement the same high-level anti-overclaim rule in different languages:

- **RQIR:** unresolved comparator degeneracy blocks `QG-007`;
- **KMQGB:** unresolved known-framework / same-realization obligations block `NEW_REQUIRED`.

## 6. Important difference in what the two routes optimize

The routes are not duplicates.

### RQIR-only reconstruction

Primary object:

- operational gravity-interface hierarchy;
- relational observables;
- mean/noise/ordered response/higher/process structure;
- detector-facing comparator quotient.

Current endpoint:

- an admissible equivalence class with a C5 witness.

### KMQGB polygon

Primary object:

- known-model proving ground;
- same-realization provenance;
- hard normalized gravity observables;
- parent-principle / functional-rigidity search;
- Paper-IV `NEW_REQUIRED` decision procedure.

Current endpoint:

- benchmark methodology complete, but scientific closure and candidate promotion not authorized.

The difference is productive: RQIR demonstrates **logical underdetermination from the operational side**, while KMQGB demonstrates **parent-selection/provenance underdetermination from the model-space side**.

## 7. No contradiction requiring RQIR thaw

No current KMQGB authority contradicts the frozen RQIR-v0 result.

In particular, KMQGB has not supplied a promoted parent principle that:

1. is finite/rigid rather than conditional on unselected functional data;
2. produces a hard gravity observable from the same realization;
3. survives the comparator quotient;
4. establishes a C5 obstruction or authorized `NEW_REQUIRED` decision.

Therefore the RQIR v0 freeze remains valid and must not be modified.

## 8. Full comparison reopen condition

A true model-to-model blind comparison becomes authorized only when the polygon route freezes a concrete Candidate Gravity/QGR authority with at least:

- explicit state space / primitive data;
- one coherent dynamics or generating law;
- fixed parameter/function count;
- hard and low-energy predictions;
- C5 comparator statement;
- declared status under the KMQGB promotion gates.

At that point compare it as the **last row** using the already frozen RQIR rules; do not tune the rules to the candidate.

## 9. Checkpoint conclusion

Current independent convergence is stronger than either route alone:

\[
\boxed{
\begin{array}{c}
\text{RQIR-only: no C5 obstruction from frozen premises}\\
+\\
\text{KMQGB: no authorized unique polygon parent / NEW_REQUIRED decision}\\
\Downarrow\\
\text{new microscopic QGR is presently not justified by either route.}
\end{array}}
\]

This is not evidence that no new quantum-gravity theory exists. It is evidence that the current two independent reconstruction procedures have **not yet earned one without an additional physical principle or empirical residual**.
