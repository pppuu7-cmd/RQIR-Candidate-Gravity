# Wave 29 — F2 Central Effective-Action Reconstruction / Projection-Rank Audit

Status: **PREREGISTERED BEFORE GITHUB COMPUTE**

## Frozen authorities

- RQIR Core v1.0 remains frozen.
- Wave-22 bank remains immutable: `holdouts/WAVE22_FROZEN_BANK.json`, blob `c32bd52edd003589ef65e0240c757475f215f55f`.
- Wave-28 certificate remains the authority for the missing 6x3 UV-to-RQIR Jacobian contract.
- F2 evidence is frozen in `evidence/WAVE29_F2_EFFECTIVE_ACTION_BASIS.json`.
- Polygon/KMQGB-derived QGR equations and architecture remain forbidden inputs.

## Scientific question

How much independent six-coordinate RQIR information is actually contained in the published central F2 effective action, once multiple 3-point and 4-point observations generated by the same diffeomorphism-invariant form-factor basis are prevented from being double-counted as independent theory directions?

## Scope boundary

F2 reconstructs two quadratic-curvature form factors from TT 3-point and 4-point momentum dependence. This wave does not assume a one-to-one map from those form factors to `[c3,d3,e4,f4,s2,s0]`. The Wave-22 coordinates are frozen finite-proxy coordinates. The audit therefore distinguishes:

1. operator/form-factor basis rank;
2. low-energy coefficient rank under declared finite truncations;
3. image rank in probe space;
4. identifiability of a map into the six frozen proxy coordinates.

## Tests and preregistered signals

### W29-1 F2 quadratic operator basis

Encode the TT incidence matrix for the two quadratic-curvature form factors across 3pt and 4pt access:

- `R^2`: 3pt = 0, 4pt != 0;
- `Ricci^2`: 3pt != 0, 4pt != 0.

Signal: `f2_quadratic_curvature_operator_rank_is_2`.

### W29-2 Wave-22 sector visibility

Read the frozen bank without modification and recover its support pattern:

- 2pt -> `s2,s0` only;
- 3pt -> `c3,d3,s2,s0`;
- 4pt -> all six coordinates.

Signal: `wave22_sector_visibility_matches_frozen_bank`.

### W29-3 Anti-overcounting theorem check

Generate synthetic probe maps from a latent rank-2 quadratic basis and from a latent rank-4 constant-plus-first-slope basis. Add arbitrarily many 3pt/4pt observable rows but keep them deterministic functions of the same latent basis.

Signals:
- `multiple_npoint_rows_do_not_raise_rank_above_latent_rank`;
- `three_and_four_point_inputs_do_not_imply_four_independent_residual_directions`.

### W29-4 Low-energy truncation rank

Test two frozen finite parameterizations of the F2 quadratic sector:

- constants-only: `[g_Ricci2,g_R2]`, rank <= 2;
- constants plus first slopes: `[g_Ricci2,g_R2,c1,c2]`, rank <= 4.

The generalized-EH sector receives no automatic residual credit because baseline subtraction may remove/absorb it.

Signals:
- `constants_only_quadratic_rank_le_2`;
- `constants_plus_first_slopes_rank_le_4_before_generalized_EH`.

### W29-5 Higher-curvature gap

The F2 paper acknowledges possible `p^6 ~ R^3` contributions but explicitly does not include them in the reconstruction used here.

Signal: `p6_R3_sector_not_available_in_F2_reconstruction`.

No particular Wave-22 proxy coordinate is declared to be an `R^3` coordinate. Only the generic six-direction projection claim is restricted.

### W29-6 Six-coordinate projection identifiability

Construct families of latent-to-proxy maps consistent with the same sector visibility and latent rank. Demonstrate that without an explicitly published F2-to-Wave22 projection, multiple inequivalent six-coordinate embeddings remain possible while producing the same latent operator rank.

Signals:
- `F2_to_wave22_six_coordinate_projection_nonunique_without_extra_map`;
- `central_f2_information_does_not_close_frozen_six_direction_parent_law`.

### W29-7 Information firewall

No polygon-derived QGR data may occur in the Wave-29 code/evidence inputs.

Signal: `information_firewall_pass`.

## Aggregate scientific boundary

A clean Wave 29 may show that the F2 central effective action provides a strong, reusable, correlated low-energy object while still being insufficient to establish six independent RQIR residual coordinates. It must not be interpreted as a failure of F2 or asymptotic safety.

Even if the central projection were fully specified, Wave 28 still requires independently displaced same-realization UV trajectories for the physical 6x3 Jacobian J8 and corresponding J9 uncertainty.

## Promotion prohibition

Wave 29 cannot by itself promote the candidate to a new parent law. It can only classify which central information is available and how much independent rank it can legitimately support.