name: RCG Iter069 G71 unresolved architecture discriminators

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/iter069_g71_unresolved_architecture_discriminators.yml'
  workflow_dispatch:

jobs:
  stream:
    strategy:
      fail-fast: false
      matrix:
        stream: [B, C, D, E]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --upgrade pip sympy
      - name: Run frozen stream ${{ matrix.stream }}
        run: |
          mkdir -p out
          python scripts/iter069_g71_unresolved_architecture_discriminators.py --stream '${{ matrix.stream }}' --out 'out/${{ matrix.stream }}.json'
      - uses: actions/upload-artifact@v4
        with:
          name: g71-${{ matrix.stream }}
          path: out/${{ matrix.stream }}.json
          if-no-files-found: error

  aggregate:
    needs: stream
    if: always()
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --upgrade pip sympy
      - uses: actions/download-artifact@v4
        with:
          pattern: g71-*
          path: downloaded
          merge-multiple: false
      - name: Frozen aggregate
        run: |
          mkdir -p out
          python scripts/iter069_g71_unresolved_architecture_discriminators.py --aggregate-dir downloaded --out out/g71_aggregate.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: g71-aggregate
          path: out/g71_aggregate.json
          if-no-files-found: error
