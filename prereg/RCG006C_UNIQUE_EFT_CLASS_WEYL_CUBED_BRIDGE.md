# RCG-006C — exact bridge from the unique RCG006 EFT quotient class to parity-even Weyl-cubed

Date: 2026-09-17
Status: **PROSPECTIVELY FROZEN BEFORE ANY EXACT WEYL-CUBED COORDINATE / MEMBERSHIP OUTCOME**

Parent RCG006 L0 map terminal: `results/RCG006_MFR_L0_MAP_TERMINAL.md`, commit `12ca52947d1390a494634b7a6a2d98d9a7e4bf7d`.

Parent exact facts:
- `dim Q_RCG005 = 8`;
- `rank(M_FR)=7`;
- `dim Q_EFT=1`;
- canonical image span hash `7c5e4b07d95881b907ed67a55742466c2889e99c20193806b834370a00f46788`.

This gate adds no generator and does not change RCG005 or RCG006-v0 classifications. It is an interpretation/identity bridge for the already-existing one-dimensional quotient.

## External source authority

Primary methodological/source anchor:
Maximilian Ruhdorfer, Javi Serra, Andreas Weiler, *Effective Field Theory of Gravity to All Orders*, arXiv:1908.08050.

Relevant source facts to be checked against the exact repository calculation:
- in 4D gravitational EFT, occurrences of `R_ab` and `R` are removable by appropriate field redefinitions, leaving Weyl tensor building blocks in vacuum;
- the parity-even cubic Weyl invariant can be written as

`I_C3 = C_{mu nu}^{rho sigma} C^{mu nu alpha beta} C_{alpha beta rho sigma}`;

- parity-odd dual-Weyl structures are outside the present parity-even RCG006 scope.

The source is interpretive authority only. It does not determine repository coordinates or the bridge outcome.

## Hypothesis

The unique nonzero class in the frozen one-dimensional quotient

`Q_EFT = Q_RCG005 / Im(M_FR)`

is exactly the class generated by the parity-even Weyl-cubed invariant `I_C3`.

## Exact object

Construct the four-dimensional Weyl tensor mechanically from the generic algebraic Riemann tensor used by the canonical RCG004 Constructor:

`C_abcd = R_abcd - (g_ac R_db - g_ad R_cb - g_bc R_da + g_bd R_ca) + (R/3)(g_ac g_db - g_ad g_cb)`.

Before using it, verify exact tracelessness and Riemann symmetries.

Then mechanically contract

`I_C3 = C_ab^{cd} C^{ab ef} C_efcd`

in the same generic 4D Euclidean algebraic-curvature parametrization used for exact quotient identities. No remembered Riemann-cubic decomposition is allowed.

Express `I_C3` exactly in the frozen six-dimensional RCG004 cubic quotient basis with canonical class indices `[0,1,2,4,5,8]`, then embed it in the frozen RCG005 eight coordinates as axes `2..7`.

## Frozen checks

Required before any scientific bridge verdict:
1. Weyl trace vanishes identically in exact generic-curvature coordinates.
2. Weyl tensor satisfies the same pair antisymmetry, pair exchange and algebraic Bianchi identities.
3. `I_C3` lies exactly in the frozen RCG004 six-dimensional quotient span.
4. The frozen RCG004 parent basis/hash identities are unchanged.
5. RCG006 `Im(M_FR)` is read only from `results/raw/RCG006_MFR_L0_CANONICAL.json`; no refit of the image is allowed.

## PASS

`PASS_SCOPED_RCG006C_UNIQUE_EFT_CLASS_IS_PARITY_EVEN_WEYL_CUBED`

iff all checks pass and:
- `I_C3` is not in `Im(M_FR)`;
- `rank([Im(M_FR), I_C3]) = 8`;
- therefore `[I_C3]` spans the one-dimensional `Q_EFT`;
- an exact proportionality between `[I_C3]`, `[D1D1_CLASS_11]` and `[RCG004_CANONICAL_AXIS_8]` modulo `Im(M_FR)` is persisted.

## FAIL

`FAIL_SCOPED_RCG006C_WEYL_CUBED_DOES_NOT_SPAN_UNIQUE_EFT_CLASS`

iff the mechanically constructed `I_C3` is in `Im(M_FR)` or fails to span the frozen one-dimensional complement while all object/parent checks remain valid.

## BLOCKED / INVALID

- Weyl object or contraction cannot be made exact under the frozen generic-curvature representation => `BLOCKED_RCG006C_WEYL_OBJECT_DEFINITION`.
- parent basis/hash mismatch, stale image, failed tracelessness/symmetry identity, or implementation disagreement => `INVALID_RCG006C_WEYL_BRIDGE`.

## Interpretation ceiling

PASS would identify the unique local parity-even pure-metric first-order **bulk EFT** class at this frozen engineering dimension with Weyl-cubed. It would not establish a phenomenologically viable correction, ghost freedom, stability, causality, quantum completion, boundary equivalence, or a new theory.

No `chi_ABC` computation is authorized.

`chi_ABC = UNAUTHORIZED_NOT_COMPUTED`.
`THEORY_ESTABLISHED = 0%`.
