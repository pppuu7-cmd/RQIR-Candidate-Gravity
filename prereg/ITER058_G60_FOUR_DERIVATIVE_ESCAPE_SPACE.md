# Iter058 / G60 — local four-derivative linearized escape-space audit

Preregistered: 2026-09-13, **before implementation and production evidence**.

## Scientific question

After G59 established uniqueness up to normalization inside the frozen local quadratic exactly-two-derivative single-symmetric-tensor class, determine whether a nontrivial **local four-derivative linearized gauge-invariant structural escape space** exists around 4D Minkowski without adopting any candidate action.

This gate is candidate-independent. It maps mathematical freedom only; it does not authorize a deformation, physical coefficient, new physics, nonlinear completion or quantum theory.

## Frozen scope

- Dimension: 4.
- Background: Minkowski metric `diag(-1,1,1,1)`.
- Field: one symmetric tensor `h_ab`.
- Linear gauge transformation: `h_ab -> h_ab + k_a xi_b + k_b xi_a` in Fourier variables.
- Local quadratic four-derivative scalar basis generated by the linearized curvature invariants `Riemann^2`, `Ricci^2`, `R^2`.
- No imported candidate coefficients or conclusions from RQIR/KMQGB/QGR.
- Overall normalizations/sign conventions may differ, but only exact rank/nullspace/projective statements count.

## Frozen streams

### A — exact invariant-basis / Gauss-Bonnet relation
For a fixed preregistered exact rational momentum panel
`k in {(1,2,3,5),(2,1,4,3),(3,5,2,1),(1,3,2,4)}`
and a deterministic integer symmetric-tensor probe basis, build exact quadratic forms for the three curvature-squared invariants. Stack their independent coefficients across the panel.

PASS iff:
1. the span rank of `{Riemann^2,Ricci^2,R^2}` is exactly `2`; and
2. the exact one-dimensional relation is projectively the 4D Gauss-Bonnet ray `(1,-4,1)`.

### B — held-out linear-gauge invariance / false-positive control
Held-out momenta:
`{(2,3,5,7),(3,1,5,2),(4,3,2,1),(5,2,1,3),(1,4,5,2),(2,5,3,1)}`.
For deterministic integer `h` and `xi` probes, compare all three curvature-squared forms before/after the exact gauge shift.

PASS iff all held-out gauge differences are exactly zero and a preregistered non-gauge control perturbation changes at least one invariant in every held-out case.

### C — conserved-source identifiability of the surviving directions
For a fixed off-null momentum panel, construct the exact conserved symmetric-source subspace `k_a T^{ab}=0`. Use two independent representatives of the quotient curvature-squared space, frozen as `R^2` and `Ricci^2`, and evaluate their exact linearized Euler/Hessian responses on deterministic conserved-source probes.

PASS iff the stacked two-direction response matrix has exact rank `2`; a deliberate duplicated-direction control must have rank `1`.

This is identifiability of structural directions only, not a claim that either direction is physically viable.

### D — basis covariance / rank-deficient control
Repeat A/C after three fixed invertible integer coordinate changes on symmetric-tensor probe coordinates and after three fixed invertible changes of the two surviving invariant coordinates.

PASS iff rank/nullity and projective Gauss-Bonnet relation are unchanged in all valid transforms, while a preregistered rank-deficient probe subset is detected as insufficient rather than falsely returning the full result.

## Aggregate interpretation rule

All A/B/C/D must be structural-valid and PASS.

If all pass, terminal classification:
`FOUR_DERIVATIVE_LINEARIZED_GAUGE_INVARIANT_ESCAPE_SPACE_TWO_DIMENSIONAL_SCOPED`.

If any mathematically valid frozen predicate fails, classification is `SCIENTIFIC_FAIL_G60_<STREAM>` and the failed frozen statement is retained. Technical/runtime errors are `INFRASTRUCTURE_OR_IMPLEMENTATION_FAIL` and may only receive a minimal repair that leaves this preregistration unchanged.

## Claim ceiling

Even a full PASS establishes only a two-dimensional local four-derivative **linearized structural** escape space modulo the Gauss-Bonnet/boundary relation on the frozen exact audit. It does not select coefficients, prove ghost-freedom/stability/unitarity, provide nonlinear completion, establish a quantum measure, or raise `THEORY_ESTABLISHED` above 0%. Programme readiness remains 66% unless a separately defined scored rubric is closed later.