# RCG014 parent-local classical-law object authority requirements — prospective preregistration

Status: `PREOUTCOME_FROZEN_REQUIREMENTS_ONLY`

Gate:
`RCG014_PARENT_LOCAL_CLASSICAL_LAW_OBJECT_AUTHORITY_REQUIREMENTS_GATE`

Prospective source authority for this preregistration:
- repository: `pppuu7-cmd/RQIR-Candidate-Gravity`
- frontier file: `recovery/CURRENT_FRONT.md`
- frontier blob: `d4489eb373be15501082f49409eaf45835cadf51`

## Frozen question

What exact content-addressed provenance/object package must exist before any object can receive the repo-local credit
`PARENT_LOCAL_EXACT_SELECTED_OBJECT`?

## Frozen scope

This gate is requirements-only. It does not construct, select, infer, reconstruct, or evaluate a parent source/classical-law object.

Under repo-only operation:
- do not fetch any successor repository;
- do not reconstruct RHPI from memory;
- do not fabricate missing source content;
- do not import dynamics or coefficients from other candidate projects;
- do not compute `chi_ABC`;
- do not derive or select `alpha`;
- do not treat `BLOCKED` as `FAIL`;
- do not treat green CI as science;
- do not promote a finite witness to a theorem.

## Frozen authority package required for `PARENT_LOCAL_EXACT_SELECTED_OBJECT`

A future object may receive that credit only if the then-current repository authority supplies, explicitly and content-addressably:

1. **Exact provenance identity** — repository-local source path(s), exact ref/commit and blob hash(es) for every authoritative defining source.
2. **Exact selected object identity** — a verbatim mathematical/object definition sufficient to distinguish the selected object from alternatives, with no reconstruction from memory.
3. **Source/input space** — the exact admissible source or state space on which the object is defined.
4. **Physical/domain scope** — the exact domain of validity and any boundary/regularity assumptions required by the definition.
5. **Transformation/equivalence law** — the exact covariance, gauge, field-redefinition, or other equivalence rule under which the object is defined.
6. **Causal/ordering rule, if applicable** — the exact ordering/causal prescription or an explicit statement that none is part of the object.
7. **Normalization/equivalence class** — the exact normalization convention or exact quotient/equivalence class needed to make the selected object unambiguous.
8. **Generating/response rule** — the exact rule that maps the authorized inputs to the parent-local classical-law/source response object.
9. **Candidate independence statement** — an explicit authority statement establishing whether and in what precise sense the object is independent of downstream gravity-candidate outcomes.
10. **Successor firewall** — explicit proof-of-provenance that no successor-only structure is silently imported as parent authority.
11. **Outcome firewall** — explicit preoutcome provenance showing that the selected object was not chosen using desired `alpha`, Weyl-cubed sensitivity, `chi_ABC`, or any downstream candidate outcome.

Missing any required item means only that `PARENT_LOCAL_EXACT_SELECTED_OBJECT` credit is not yet authorized. It is not a physics FAIL.

## Frozen classifier

- `PASS_REQUIREMENTS_PACKAGE_COMPLETE`: every required item above is explicitly present and content-addressed in the permitted repository authority.
- `BLOCKED_MISSING_REQUIRED_AUTHORITY_ITEM`: one or more required items are absent or not content-addressed.
- `INVALID_SCOPE_VIOLATION`: the audit requires successor fetching, memory reconstruction, fabricated source content, forbidden coefficient/dynamics import, or any locked downstream computation.

No numerical value, coefficient selection, bridge, response value, or `chi_ABC` computation is authorized by a PASS of this requirements gate alone.

## Claim locks retained

`alpha = UNSELECTED`.

`chi_ABC = UNAUTHORIZED_NOT_COMPUTED`.

`THEORY_ESTABLISHED = 0%`.

STOP after freezing this preregistration. No substantive RCG014 audit is executed in this run.
