# RCG-002 / RSC-QCPT1 — quantum constitution principle triage pre-outcome

Status: `PREREGISTERED_BEFORE_SUBSTANTIVE_TRIAGE`

Starting authority: `681008eb19b3ceb105b63a289dbeb630698b2c75`

## PURPOSE

RSC1 left an independent quantum-side blocker:

`BLOCKED_POSITIVE_INFLUENCE_INTERFACE_MISSING_DATUM`.

SCPT1 terminalized the source branch under current-authority principles with

`NO_PREOUTCOME_SOURCE_CONSTITUTION_SELECTED`.

Before adding a new microphysical source ontology axiom, this gate audits the orthogonal quantum blocker: whether current RCG-002/RSC commitments already select a normalized positive quantum/state/influence constitution, or whether a second independent new model-defining axiom is required.

No connected `chi_ABC`, connected noise outcome, novelty observable, preferred nonlinear coefficient or held-out connected result may be inspected or computed in this gate.

## TARGET HYPOTHESIS

At least one prospectively frozen quantum-constitution principle is sufficiently grounded in current RCG-002/RSC authority to fix the physical state/measure/influence object without an additional arbitrary connected phase/noise functional.

## FROZEN PROPOSAL SET

### Q1 — UCQ: Unified Closed Quantization

Quantize the same closed source+carrier variational/constraint constitution that would generate the classical closed dynamics; derive the operational influence object by tracing only explicitly specified unobserved degrees of freedom on a closed-time-path or equivalent normalized construction.

This proposal may not import an external candidate action, vacuum, gauge-fixed measure or renormalization prescription.

### Q2 — PCF: Pure Coherent Factorization

Require the nonlinear influence kernel to remain noiseless/rank-one whenever the full closed carrier is not physically discarded:

`K(x,y)=exp(i[Phi_x-Phi_y])`.

No extra stochastic/decohering sector is introduced. The principle passes only if current authority also fixes the physical phase functional `Phi` rather than merely its rank-one form.

### Q3 — GQC: Gaussian Quadratic Closure

Extend the validated free-linearized Gaussian comparator structure by requiring the logarithm of the influence functional to terminate at quadratic order in source histories, with all higher connected cumulants zero unless independently required by current authority.

This is evaluated as a proposed minimality principle, not imported standard physics and not assumed to describe nonlinear RSC.

### Q4 — MPE: Minimum Positive Extension

Among normalized positive/CPTP extensions that reproduce the frozen lower-order channel data, select the extension with minimum additional decoherence/noise relative to the inherited lower-order map. This principle passes only if current authority supplies a basis/reparameterization-invariant minimization functional and the minimizer fixes both coherent and incoherent higher-order freedom.

## FROZEN MANDATORY CRITERIA

C1 `CURRENT_AUTHORITY_GROUNDING` — motivated by RCG-002/RSC/contract rather than imported candidate dynamics or desired connected outcome.

C2 `PHYSICAL_QUANTUM_OBJECT_FIXED` — Hilbert/state/influence/CPTP object and its physical domain are specified, not merely an abstract PSD matrix.

C3 `NORMALIZATION_POSITIVITY` — exact normalization and positivity/CPTP or an explicitly justified physical alternative.

C4 `SOURCE_HISTORY_DOMAIN` — admissible source/history inputs and branch preparation domain are fixed.

C5 `CAUSAL_CTP_RULE` — retarded/CTP/causal prescription and contour/boundary conditions are fixed or supplied by a fully specified test route.

C6 `STATE_MEASURE_NOISE_LAW` — initial state, measure and any noise/environment law are fixed pre-outcome; no arbitrary state distribution may be chosen later.

C7 `WEAK_FIELD_PAIRWISE_RECOVERY` — exact route back to the validated lower-order weak-field/pairwise channel.

C8 `RSC_NONLINEAR_COMPATIBILITY` — the proposal is capable of belonging to a nonlinear RSC descendant rather than merely restating the free linearized comparator.

C9 `NO_FREE_CONNECTED_DATUM` — no arbitrary higher-order phase, cumulant, kernel coordinate or noise function remains available for post-hoc selection.

C10 `REPRESENTATION_ROBUSTNESS_INDEPENDENCE` — no basis-dependent minimization, hidden normalization, or imported external-candidate dynamics; must PASS or have an exact pre-outcome test route.

C1-C9 are mandatory PASS conditions. C10 must PASS or have a fully specified exact test route.

## FROZEN FALSE-POSITIVE CALIBRATIONS

### Calibration A — fixed microscopic unitary does not by itself fix the influence state

For branch/history labels `x`, freeze any family of environment unitaries `U_x` and any density operator `rho_E`. Define

`K_rho(x,y)=Tr[rho_E U_y^dagger U_x]`.

The gate will verify, without choosing a connected outcome, that every such `K_rho` is normalized and PSD while different admissible `rho_E` can in general yield different kernels for the same `U_x`. Therefore a proposal of the form “quantize the dynamics” is insufficient unless it also fixes the physical state/measure.

### Calibration B — pure coherent rank-one form does not by itself fix higher phase

For any phase assignment `Phi_x`,

`K(x,y)=exp(i[Phi_x-Phi_y])`

is normalized rank-one PSD. The gate may test whether adding a real phase deformation that vanishes on all frozen lower-order histories preserves those lower-order data while changing only unfixed higher-history phases. No connected observable value is to be calculated.

### Calibration C — positivity/minimum-noise does not automatically fix coherent freedom

Existing NP1/NCP1 structural authority may be used only to test whether normalized PSD extensions can share the same lower-order data while differing in coherent and/or incoherent higher-order coordinates. No new arbitrary channel/kernel scan is authorized.

## PASS / FAIL / BLOCKED / INVALID

### PASS

`RSC_QUANTUM_CONSTITUTION_SELECTED_PREOUTCOME`

At least one frozen proposal passes all mandatory criteria without importing external candidate dynamics and without leaving a free connected datum.

### NO-SELECTION TERMINAL

`NO_PREOUTCOME_QUANTUM_CONSTITUTION_SELECTED`

Every frozen proposal fails at least one mandatory criterion for a reason demonstrated from current authority or the frozen calibrations.

### BLOCKED

`BLOCKED_QUANTUM_CONSTITUTION_OBJECT_UNDEFINED`

The gate cannot even evaluate a proposal because a prerequisite object required by the frozen criteria is absent and no scoped structural verdict is possible.

### INVALID

Chronology violation, connected-outcome inspection, post-hoc proposal/criterion change, imported candidate dynamics, or substitution of a synthetic calibration as RCG-002 physics.

## INTERPRETATION CEILING

This is a finite pre-outcome proposal triage. `NO_PREOUTCOME_QUANTUM_CONSTITUTION_SELECTED` would not prove that no quantum constitution can exist. It would show only that current RCG-002/RSC authority does not select one among the frozen high-value proposals.

No result may authorize `NEW_PHYSICS_FOUND`, `FULL_QUANTUM_GRAVITY`, `THEORY_ESTABLISHED`, a universal classical/semiclassical/noisy no-go, family-wide uniqueness, or computation/fitting of `chi_ABC`.

Programme readiness remains 66% and theory established remains 0% unless a separate repository authority changes them.
