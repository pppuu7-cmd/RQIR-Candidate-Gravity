# RCG008 execution-only wording repair freeze

Status: PROSPECTIVELY FROZEN BEFORE REPAIRED SCIENTIFIC EXECUTION

The first Actions attempt `35367595336` at head `b01f237d9d50601805ca2dc8e84cc63db427f491` terminated before any RCG008 scientific census result:
- Constructor stopped on the literal G90 evidence anchor `not a physical nonlinear completion`;
- independent Critic stopped on the same literal anchor;
- aggregate and auditor were skipped;
- execution identity verification passed in both lanes.

A bounded pre-rerun static audit of every frozen evidence anchor found exactly two literal mismatches:
1. G90 source contains `physical nonlinear completion`, not the stricter literal `not a physical nonlinear completion`;
2. Constructor G96 included `constraint attribution`, while the terminal authority instead contains the already-frozen `diagnostic connected directions` anchor.

Authorized repair only:
- G90 evidence anchor -> `physical nonlinear completion` in Constructor, Critic and Auditor;
- remove the absent redundant G96 Constructor literal while retaining `diagnostic connected directions`;
- add the dedicated repair-freeze trigger path to the existing workflow.

Locked unchanged:
- RCG008 preregistration and question;
- corpus manifest and every source/dependency blob identity;
- object set;
- perturbative-order, ownership, candidate-independence, domain, applicability, bridge, synthetic and qualification decisions;
- positive-control expectations;
- aggregate classifier and four-outcome terminal taxonomy;
- `alpha = UNSELECTED`;
- `chi_ABC = UNAUTHORIZED_NOT_COMPUTED`;
- `THEORY_ESTABLISHED = 0%`.

The failed attempt is infrastructure-only and is not a scientific RCG008 outcome.
