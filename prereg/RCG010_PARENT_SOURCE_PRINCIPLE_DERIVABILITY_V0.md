# RCG010 — parent RQIR source/generating/influence principle derivability gate — preregistration v0

Status: **PROSPECTIVELY FROZEN BEFORE EXECUTION**

Frontier: `2cd66deb88f513a36ca78b00de5e6c15426e1dd5`.

Parent terminal authority: RCG008 and RCG009 remain terminal and are not reopened.

## Frozen scientific question

`CAN_A_CANDIDATE_INDEPENDENT_RQIR_PARENT_SOURCE_GENERATING_OR_INFLUENCE_PRINCIPLE_BE_DEFINED_OR_DERIVED_WITH_EXACT_SOURCE_SPACE_DOMAIN_AND_TRANSFORMATION_LAW_WITHOUT_USING_GRAVITY_CANDIDATE_OUTCOMES?`

This gate is **not** a third-variation or value gate. It does not compute a cubic response, choose a Weyl-cubed bridge, evaluate alpha sensitivity, solve alpha, compute `chi_ABC`, or expand the gravity candidate.

## Authority layers

The frozen corpus is recorded machine-readably in
`data/RCG010_PARENT_SOURCE_PRINCIPLE_DERIVABILITY_AUTHORITY_V0.json`.

The gate separates:

1. **Inherited parent primitives** — relational controlled-phase operational seed; construction/consistency contract; linearized conserved-source retarded spin-2 baseline; G97 closed source/preparation bookkeeping.
2. **Parent structural terminals** — RSC1, SSE1/CSA1/SCPT1, QCPT1, MAPF1, AB1 and S2PA1, used only for already-terminal sufficiency/underdefinition facts.
3. **Successor-only information** — RQIRCGSF source/principle work is not retroactive parent authority.
4. **Gravity-candidate outcomes** — forbidden as selectors.

No external equation, standard model, GR/QFT candidate dynamics, G89 D-specific object, Weyl-cubed outcome, coefficient value, or connected response is admissible evidence for parent-principle selection.

## Recovered inherited primitive commitments

The gate recognizes the following as inherited commitments only within their exact scopes:

- a relational operational channel architecture;
- a requirement that microscopic source/distance/time/`G`/`hbar` dependence be derived rather than fitted;
- gauge/relational observable discipline;
- normalized quantum-state evolution and causal/consistency obligations;
- a linearized symmetric spin-2 field with conserved `T_{mu nu}` and retarded response in the baseline scope;
- exact closed probe+apparatus/support momentum bookkeeping in G97.

These commitments are constraints and prerequisites. They are not silently promoted into a nonlinear source ontology, a complete local action, a state/measure law, a parent generating functional, or a canonical equivalence quotient.

## Legitimate parent-principle contract

A terminal `SOURCE_DEFINED` result requires one exact candidate-independent parent principle or exact derivation rule that fixes a physical equivalence class sufficient to determine all applicable fields below **before outcomes**:

- **R1_PARENT_PRINCIPLE_OR_DERIVATION_RULE** — an explicit parent source/generating/influence principle, or a derivation theorem from inherited parent primitives, is present.
- **R2_SOURCE_SPACE** — physical source variables / index spaces / admissible source family are explicit.
- **R3_DOMAIN** — spacetime/history/operational domain is explicit and not merely D-specific, RCG002-subconstruction-specific, or gravity-candidate-specific.
- **R4_TRANSFORMATION_OR_EQUIVALENCE_LAW** — allowed source transformations, representation changes or equivalence quotient are explicit enough to determine what is physical.
- **R5_CAUSAL_OR_ORDERING_RULE** — retarded/CTP/branch/order/contact structure is explicit if the selected parent principle requires it; otherwise `NOT_APPLICABLE`.
- **R6_NORMALIZATION_OR_EQUIVALENCE_CLASS** — normalization, additive/gauge redundancy, or physical equivalence class of the parent object is explicit.
- **R7_GENERATING_OR_RESPONSE_RULE** — the principle actually maps the source/state/preparation data to a generating/influence/response object rather than stating only admissibility constraints.
- **R8_CANDIDATE_INDEPENDENCE** — the principle remains defined when Weyl-cubed and all gravity-candidate completion outcomes are removed.
- **R9_SUCCESSOR_FIREWALL** — successor-only source principles are not retroactively promoted.
- **R10_PREOUTCOME_SELECTION** — no `chi_ABC`, alpha, connected sign/magnitude, or desired candidate behavior is used to select the principle.

## Cheapest obstruction first

The execution must test **R1 first**.

If inherited parent authority contains neither:
- an explicit complete parent principle, nor
- a derivation/selection rule that fixes one physical parent-principle equivalence class,

then the gate terminalizes

`BLOCKED_SCOPED_RCG010_EXISTING_RQIR_PRINCIPLES_UNDERDETERMINE_PARENT_SOURCE_PRINCIPLE`

and must **not** fill R2-R7 by convention, by “minimality”, by “standard” source choices, by a convenient state, by an external gravity model, or by successor-only principles.

If R1 passes but any downstream contract field is unresolved, terminalize

`BLOCKED_SCOPED_RCG010_SOURCE_SPACE_DOMAIN_OR_TRANSFORMATION_LAW_UNRESOLVED`.

## Frozen control families

The independent Constructor and Critic must recover these controls without weakening them:

- **Closed variational source**: CSA1/SCPT1 prevent generic “one closed action / Hilbert stress / minimal coupling” language from counting as a unique parent source principle while source ontology, field basis or curvature-coupling rules are unresolved.
- **Positive influence**: RSC1/QCPT1 prevent positivity/CPTP/coherent factorization from selecting the state/measure/influence law.
- **Relational/composition wording**: S2PA1 prevents “relational” or path-independence language from silently importing a complete canonical/action premise class.
- **Formal package**: MAPF1 prevents a schema containing placeholders from counting as selected model content.
- **Successor firewall**: RQIRCGSF future source principles are not historical parent authority.
- **Synthetic complete principle**: a fully specified synthetic parent principle with explicit source space/domain/transformation/ordering/normalization/generating rule must take the `SOURCE_DEFINED` classifier branch. This is a classifier control only.

## Frozen terminal taxonomy

Exactly one terminal classification:

1. `SOURCE_DEFINED_SCOPED_RCG010_PARENT_SOURCE_PRINCIPLE_DERIVED_FROM_EXISTING_RQIR_AUTHORITY`
2. `BLOCKED_SCOPED_RCG010_EXISTING_RQIR_PRINCIPLES_UNDERDETERMINE_PARENT_SOURCE_PRINCIPLE`
3. `BLOCKED_SCOPED_RCG010_SOURCE_SPACE_DOMAIN_OR_TRANSFORMATION_LAW_UNRESOLVED`
4. `INVALID_RCG010_IMPLEMENTATION_OR_PROVENANCE`

No new outcome category may be added after execution.

## Claim locks

Maintain exactly:

- `A = span{alpha}`;
- `selector_rank = 0`;
- `residual_dimension = 1`;
- `PARENT_FUNCTIONAL_DEFINED = NO` unless this gate terminally SOURCE_DEFINED and a later separate source-object gate actually defines it;
- `SOURCE_OBJECT_DEFINED = NO`;
- `VALUE_DERIVATION_AUTHORIZED = NO`;
- `BRIDGE_AUTHORIZED = NO`;
- `ALPHA_SENSITIVITY_AUTHORIZED = NO`;
- `alpha = UNSELECTED`;
- `chi_ABC = UNAUTHORIZED_NOT_COMPUTED`;
- `THEORY_ESTABLISHED = 0%`.

Even if RCG010 SOURCE_DEFINED, **STOP**. A parent functional/source object would still require a separate prospective definition gate before any value derivation.

If RCG010 BLOCKED, the next admissible move is a separately frozen **new-axiom parent-principle formation/selection gate**, explicitly labeled new model-defining information rather than inherited RQIR content.

