# RCG002-VB1 — current-version generative-axiom sufficiency audit

Date: 2026-09-14
Status: PROSPECTIVELY FROZEN BEFORE SUBSTANTIVE CLASSIFICATION

## STATE LOCK

Starting authoritative main: `125e937a6a37c0c17f5de0dfb27d1deed5223b28`.
Starting front: `CM1_CLOSED_MEDIATOR_NONUNIQUENESS_TERMINAL / NEW_CANDIDATE_VERSION_PRINCIPLE_FRONTIER`.
Readiness: 66%. Theory established: 0%.

This gate does not invent a new nonlinear law. It asks whether the **current RCG-002 version already contains one**.

Frozen candidate-owned authority universe for this decision:

1. `candidates/RCG_002_RELATIONAL_CONTROLLED_PHASE_CHANNEL.md`;
2. `docs/CONSTRUCTION_CONTRACT.md`;
3. `candidates/RCG_002_COVARIANT_BASELINE_COMPLETION_V0.md`;
4. `results/ITER095_G97_CLOSED_TOTAL_SOURCE_PREPARATION_TERMINAL.md`;
5. `results/RCG002_NP1_CONNECTED_CHANNEL_EXTENSION_TERMINAL.md`;
6. `results/RCG002_CM1_CLOSED_SEQUENTIAL_MEDIATOR_TERMINAL.md`;
7. `recovery/CURRENT_FRONT.md` at the starting main.

No physical assumptions, equations, coefficients, or preferred dynamics from QGR, MSQGR/CRQN, KMQGB, RQIR, ISQGR, or another candidate repository may be imported as a selector.

## HYPOTHESIS

`H_VB1`: the current RCG-002 version does **not** contain a candidate-owned physical source/state/evolution principle that determines the nonlinear connected response beyond the validated pairwise/linearized branch. If this is correct, current-version nonlinear dynamics must be terminally classified as underdetermined, and any added law that fixes the connected freedom must be labelled a prospectively new candidate version.

The opposite result is allowed: if an already-authoritative current-version statement supplies such a law, VB1 must identify it and must **not** terminalize the version as underdetermined.

## DECISION OBJECT

The decision is logical/structural, not a coefficient fit.

Define a `QUALIFYING_GENERATIVE_PRINCIPLE` as an already-authoritative current-version statement or finite set of statements that, without adding a new free connected coefficient/function or a new dynamical postulate, does all of the following at the nonlinear frontier:

1. identifies the physical state/field object on which the rule acts;
2. identifies the closed physical source/preparation input or its map into that state/field object;
3. supplies an evolution/influence equation or rule beyond the purely linear/Gaussian baseline;
4. determines at least one genuinely connected nonlinear observable (phase and/or coherence/noise response) modulo the already-established <=2-body phase quotient;
5. fixes that connected observable from the supplied source/state data rather than leaving an arbitrary `lambda`, `gamma`, mediator phase, kernel function, or equivalent free connected datum.

Full nonlinear Bianchi/microcausal closure is **not required merely to count a rule as generative** in VB1; those are downstream consistency obligations. VB1 therefore uses the weakest useful definition. If no rule passes even this weaker test, it cannot already be a full physical nonlinear completion.

## NEW INFORMATION SOUGHT

NP1 and CM1 established counterexamples to specific structural sufficiency claims. They did not by themselves exhaustively classify every authoritative statement in the current RCG-002 version as generative vs non-generative.

VB1 supplies that missing version-boundary theorem by combining:

- an exhaustive authority inventory;
- an exact linear-baseline control;
- prior countermodels as model-theoretic witnesses;
- a prospective rule for when a new law necessarily constitutes a new candidate version.

## LANE A — AUTHORITY / GENERATIVE-PRINCIPLE INVENTORY

For every dynamical or constraint-bearing statement in the frozen authority universe, classify it into exactly one of:

- `OPERATIONAL_DEFINITION`;
- `ADMISSIBILITY_CONSTRAINT`;
- `LINEAR_BASELINE_GENERATOR`;
- `SOURCE_PREPARATION_PREREQUISITE`;
- `NONUNIQUENESS_RESULT`;
- `QUALIFYING_NONLINEAR_GENERATIVE_PRINCIPLE`;
- `FUTURE_REQUIREMENT_OR_MISSING_OBJECT`.

A statement may be quoted only in short form; the audit must preserve its exact scope.

Lane A passes `NO_CURRENT_NONLINEAR_GENERATOR_FOUND` iff no already-authoritative statement satisfies the frozen qualifying definition. It fails that outcome if even one qualifying current-version nonlinear generator is found.

## LANE B — LINEAR-BASELINE CONTROL

The existing covariant baseline contains a linear field equation and a Gaussian/free-field comparison. VB1 must not confuse this with absence of all dynamics.

Freeze the affine-source control

`J(a,b,c) = J0 + a A + b B + c C`

and a general quadratic source functional

`W2[J] = L[J] + (1/2) B[J,J] + const`,

where `L` is linear and `B` bilinear.

Required exact identity:

`Delta_A Delta_B Delta_C W2[J(a,b,c)] = 0`.

Positive classifier control: a synthetic cubic functional `W3[J]` with a nonzero trilinear component on `(A,B,C)` must produce a nonzero third finite difference and therefore be recognized as genuinely nonlinear information. This synthetic control is **not candidate physics** and cannot be adopted as an RCG-002 law.

Lane B classification rules:

- if the current baseline supplies only the quadratic/linearized scoped result, label it `SCOPED_LINEAR_BASELINE_CONNECTED_NULL_CONTROL`, not a nonlinear completion;
- if an already-authoritative nonlinear term/rule is discovered in the frozen authority universe, Lane B must identify it and route the gate to the opposite outcome.

No conclusion about a physical three-source signal is allowed from the quadratic null control alone.

## LANE C — MODEL-THEORETIC ENTAILMENT TEST

Use only already-validated NP1/CM1 results as countermodel premises; do not create a new arbitrary channel scan.

Frozen logic:

If two admissible realizations satisfy all current constraints tested at their stated scope but differ in a connected observable that a purported current principle is claimed to determine, then that connected observable is **not entailed by those current constraints**.

Required witnesses:

1. NP1 phase witness: choose two distinct real values `lambda_1 != lambda_2` at fixed admissible base channel and fixed `gamma`; both preserve the frozen lower faces and operational CPTP requirements while differing in connected phase.
2. NP1 noise witness: choose two distinct `gamma_1 != gamma_2 >= 0` at fixed `lambda`; both remain positive normalized Schur completions while differing in connected coherence magnitude.
3. CM1 mediator witness: choose two distinct real mediator phases `lambda_1 != lambda_2`; both use the same closed sequential mediator architecture, exact reset, same primitive type, and same lower-face recovery while differing in connected phase.

These witnesses establish only non-entailment of the tested operational/mediator constraints. They are not gravitational realizations and do not define the physical completion-space dimension.

## LANE D — VERSION-BOUNDARY LOGIC

Prospectively freeze the version rule:

- If Lanes A-C show that no current authoritative nonlinear generator exists and at least one connected observable is non-entailed by the current constraints, then any future rule `R_new` that fixes that observable cannot be presented as a result already contained in the current version. It is an additional independent postulate/derivation object and must be labelled a prospectively new RCG-002 candidate version before its connected outcome is inspected.
- If Lane A or B finds an already-authoritative qualifying generator, VB1 must not apply this version-boundary conclusion; the next gate must instead test that existing generator.

This is a logical versioning statement, not a theorem that no future physical principle can exist.

## POSITIVE CONTROLS

1. The quadratic functional must give exact third finite difference zero.
2. A synthetic cubic trilinear functional must give a nonzero third finite difference when its `(A,B,C)` coefficient is nonzero.
3. A synthetic rule that explicitly maps fixed source/state data to a unique connected coefficient with no free connected parameter must be classified by the audit machinery as `QUALIFYING_NONLINEAR_GENERATIVE_PRINCIPLE`; it remains only a classifier control and is not adopted as candidate physics.

## NEGATIVE / FALSE-POSITIVE CONTROLS

The following must **not** count as a qualifying nonlinear generator by themselves:

- `chi` exists as an operational parameter;
- complete positivity / CPTP normalization;
- covariance or gauge discipline without an evolution law;
- total spatial-translation conservation from G97;
- existence of a mediator;
- exact mediator reset;
- finite circuit ordering;
- a search-order or promotion requirement;
- a statement that a future theory must derive `chi`;
- the linearized baseline outside its stated weak-field/Gaussian scope;
- a free nonlinear ansatz with fitted coefficient.

## OUTCOME LABELS

### Outcome U — current version underdetermined

Allowed only if all are true:

- Lane A finds no qualifying current-version nonlinear generator;
- Lane B confirms that the existing explicit generator is only the scoped linear/Gaussian baseline and does not supply a nonlinear rule;
- Lane C supplies the frozen non-entailment witnesses from validated NP1/CM1;
- no source/provenance conflict invalidates the authority universe.

Classification:

`RCG002_CURRENT_VERSION_NONLINEAR_DYNAMICS_UNDERDETERMINED`

with

`CURRENT_VERSION_TERMINAL_AT_NONLINEAR_MODEL_DEFINITION_BOUNDARY`.

### Outcome G — existing generator found

If an already-authoritative statement satisfies the frozen qualifying definition:

`EXISTING_CURRENT_VERSION_NONLINEAR_GENERATIVE_PRINCIPLE_FOUND`

VB1 must identify it exactly and must not terminalize the current version. The next gate must test that rule on the same physical realization.

### Outcome B — blocked authority

If the frozen authority universe is internally inconsistent, missing a referenced authoritative file, or cannot be provenance-locked:

`BLOCKED_AUTHORITY_UNIVERSE_INCOMPLETE`.

### Outcome I — invalid audit

If the classification rules are changed after inspection, sources outside the frozen universe are imported as candidate dynamics, or NP1/CM1 witnesses are silently promoted to physical gravity:

`INVALID_VB1_CONTRACT_VIOLATION`.

## INTERPRETATION CEILING

Even Outcome U does **not** mean:

- RCG-002 can never be completed;
- nonlinear gravity is impossible;
- all classical/semiclassical/noisy models are excluded;
- a physical completion-space dimension is known;
- standard GR/EFT nonlinearities are absent;
- a new nonlinear law has been discovered;
- new physics or full quantum gravity has been established.

It means only that the **current frozen RCG-002 version** is not yet a physically determined nonlinear theory and that a law fixing the nonlinear connected response must enter as prospectively new candidate-owned information.

Readiness is frozen at 66% for this gate. Theory established remains 0%.