# RCG-008 — higher-order RQIR observable/source-authority census v0 (PREOUTCOME)

Date: 2026-09-17
Status: **PROSPECTIVELY FROZEN BEFORE CENSUS OUTCOME**

Parent scientific terminal: `356a1274b3791d11cf453169014d9ffcc230d398`.
Independent parent audit: `29b6c0c13ff6d59316a514a8fdbbaaf3c6d378b0` (`CONFIRMED_SCOPED`).
Recovery head before this preregistration: `c41bcc990570770c3142d9f9522e9d5f6a2edc99`.

## Selector type

`NECESSARY / SOURCE_AUTHORITY_DEFINITION_GATE`.

This gate does not select `alpha`. It asks whether a legally usable candidate-independent higher-order RQIR datum already exists in durable authority.

## Hypothesis

Current RCG007 leaves a one-dimensional Weyl-cubed coefficient space `A=span{alpha}` with exact inherited selector rank zero. Before expanding the gravity model class, determine whether the existing durable repository authority already contains at least one genuinely cubic / third-variation-or-higher RQIR observable, constraint, interface datum, or selection principle that can legally be used, or separately bridged, to test `alpha`.

## Census object

The object is the set of durable authority-bearing repository records, not chat history and not external invention. The bounded census searches current authoritative/recovery/result/prereg/source-style records for higher-order RQIR data relevant to the RCG007 blocker.

A record qualifies as a `CANDIDATE_HIGHER_ORDER_DATUM` only if all are true:
1. it is genuinely cubic / third-variation-or-higher in the relevant perturbative/information sense, or provides an explicitly equivalent higher-order sensitivity;
2. it is candidate-independent / upstream RQIR-owned rather than introduced by a gravity candidate under test;
3. its exact identity, scope and provenance are durably recoverable;
4. it is applicable to gravity or has a separately identifiable bridge target to the pure-metric Weyl-cubed coefficient;
5. it is not merely a synthetic control, post-hoc condition, numerical fit, naturalness prior, or convention.

## Frozen search vocabulary

Use bounded combinations of the following exact concept families:
- `cubic`, `third variation`, `third derivative`, `three-point`, `3-point`;
- `retarded`, `kernel`, `CTP`, `Ward`;
- `observable`, `source`, `interface`, `constraint`, `selector`;
- `G88`, `G89`, `G90`, `G91`;
- `gravity`, `Weyl`, `alpha`.

Inspect only returned/high-confidence authority-bearing files and their direct source/prereg/result dependencies. Do not perform an unbounded historical scan.

## Positive controls

The census implementation must find and classify at least:
- G89 genuinely cubic/retarded information, if present, as `FOUND_BUT_D_SPECIFIC_UNBRIDGED` unless current authority says otherwise;
- the RCG007 synthetic cubic sensitivity control as `FOUND_BUT_SYNTHETIC_CONTROL`;
- G88 lower-order inherited information as `FOUND_BUT_TOO_LOW_ORDER_FOR_ALPHA`.

Failure to recover these known classes invalidates coverage.

## Exclusions

The following do not qualify:
- post-hoc `alpha=0`;
- RCG002 candidate-owned linearized baseline;
- G89 D-specific dynamics absent a separate candidate-independent bridge;
- synthetic calibration rows;
- gravity-candidate-generated observables with no upstream authority;
- `chi_ABC` or any unauthorized source representative;
- new external physics invented during this census.

## Allowed classifications

- `PASS_SCOPED_RCG008_EXISTING_APPLICABLE_HIGHER_ORDER_RQIR_DATUM_FOUND` — at least one exact candidate-independent higher-order datum survives all ownership/domain/bridge filters. This does **not** select `alpha`; it only authorizes a separate prospective bridge/selector gate.
- `BLOCKED_SCOPED_RCG008_NO_EXISTING_APPLICABLE_HIGHER_ORDER_RQIR_DATUM_IN_CURRENT_AUTHORITY` — coverage controls pass but no existing record survives all filters.
- `BLOCKED_RCG008_AMBIGUOUS_SOURCE_OWNERSHIP_OR_DOMAIN` — a plausible object exists but legal ownership/applicability cannot be resolved from durable authority.
- `INVALID_RCG008_CENSUS_COVERAGE_OR_PROVENANCE` — known controls are missed, repository state is stale/inconsistent, or provenance is insufficient.

## Interpretation ceiling

A scoped `BLOCKED` means only that the current durable authority does not contain an already-usable higher-order datum under this frozen census. It is not a theorem that no such RQIR observable can ever be derived or supplied. It does not set `alpha=0`, falsify Weyl-cubed, authorize model-class expansion, or establish Candidate Gravity.

`chi_ABC = UNAUTHORIZED_NOT_COMPUTED`.
`THEORY_ESTABLISHED = 0%`.
