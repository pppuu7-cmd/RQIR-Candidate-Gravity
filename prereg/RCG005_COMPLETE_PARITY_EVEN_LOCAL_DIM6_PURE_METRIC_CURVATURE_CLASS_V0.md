# RCG-005 — complete parity-even local dimension-six pure-metric curvature class v0 (PREOUTCOME)

Date: 2026-09-17
Status: **PROSPECTIVELY FROZEN BEFORE ANY RCG005 BASIS / RANK / NULLSPACE / SURVIVOR OUTCOME**

Programme selection terminal:
`355ac8a2d892e311bd1989e7057c3defac2e7e8a`.

Scoped formation authority:
`d2e38871ee5ac2e1331934647575e4a5b6144e4a`.

Parent RCG004 scientific terminal:
`c39cbbd8a7e8c0caeb14e080344d01119b2ce7db`.

Version:
`RCG005_COMPLETE_PARITY_EVEN_LOCAL_DIMENSION_SIX_PURE_METRIC_CURVATURE_CLASS_V0`.

## Frozen scientific question

Does any nonzero member of the complete bounded four-dimensional parity-even local pure-metric engineering-mass-dimension-six correction class have off-shell metric equations of motion of derivative order no higher than two on the frozen primary geometry, with any surviving subspace also passing the prospectively frozen independent held-out test without refit?

The all-zero correction vector is not a survivor. Einstein-Hilbert plus optional cosmological constant is a fixed lower-order reference only and is not an RCG005 correction direction.

## Family boundary

Dimension: `4`.
Parity: `even`.
Source: `VACUUM_ZERO`.
Local pure metric only.
Correction engineering mass dimension: `6`.

Raw factors are schematic `nabla^{k_i} Riemann` with
`sum_i (2+k_i)=6`, all indices completely contracted using the metric after all derivative-index placements are mechanically generated.

The raw generator must include before quotienting all admissible templates, including at minimum the partitions represented schematically by:
- `Riemann^3`;
- `Riemann * nabla^2 Riemann` with every derivative/index placement allowed by tensor rank;
- `(nabla Riemann)*(nabla Riemann)` with every derivative/index placement;
- one-curvature/four-derivative structures `nabla^4 Riemann` where a scalar contraction is possible before exact identities / total derivatives are applied.

The raw class must not start from a memorized literature basis. Persist template counts, raw contraction counts and machine-readable manifests before reduction.

Excluded:
- quartic or higher curvature order / dimension-eight corrections;
- matter or external stress-energy;
- extra scalar/vector/tensor fields;
- torsion/non-metricity;
- nonlocality or inverse differential operators;
- parity-odd single-epsilon operators;
- coefficient fitting or approximate numerical nullspaces.

Double-epsilon parity-even contractions, if generated, must be reduced exactly to generalized-delta / metric contractions and checked for representation in the parity-even metric-contraction family.

## Exact equivalence convention

Allowed quotient relations only:
1. Riemann antisymmetries and pair exchange;
2. first algebraic Bianchi identity;
3. differential Bianchi identity;
4. exact covariant-derivative commutators, with their generated curvature terms retained inside the same dimension-six class;
5. exact action-level integration by parts / total derivatives;
6. exact Ricci/scalar contraction identities;
7. exact four-dimensional Schouten / dimension-dependent identities;
8. exact duplicate contractions;
9. exact double-epsilon reduction if encountered.

Forbidden in v0 equivalence:
- equations of motion;
- on-shell identities;
- background-specific identities;
- perturbative field redefinitions;
- derivative-dependent field redefinitions;
- phenomenological equivalence.

`FIELD_REDEFINITION_EQUIVALENCE = UNRESOLVED_OUT_OF_SCOPE_RCG005_V0`.

No raw operator may be removed solely because it vanishes on FRW, Bianchi-I, a conformally-flat background or a finite metric sample.

## Mechanical completeness certificate

The Constructor must persist:
- allowed template manifest;
- raw contraction manifest;
- algebraic-symmetry orbit relations;
- zero/duplicate relations;
- integration-by-parts relation matrix;
- differential-Bianchi relation matrix;
- derivative-commutator relation matrix;
- four-dimensional identity relations;
- total exact relation matrix;
- exact relation rank;
- exact quotient dimension `N`;
- exact quotient basis and transformation maps;
- a proof/certificate that every raw template allowed by the frozen engineering-dimension rule is represented.

No value of `N` is assumed in advance.

An independent Critic must reconstruct the quotient by a genuinely independent route and compare quotient spans exactly rather than require identical basis labels.

If exact rational arithmetic becomes too costly, modular acceleration is permitted only with all of the following prospectively fixed in the implementation before rank outcome: multiple fixed primes, denominator-clearing convention, rank-stability comparison, deterministic rational reconstruction and final exact substitution verification. A one-prime rank is never terminal evidence.

## Inherited RCG004 subspace

The canonical six-dimensional RCG004 algebraic cubic quotient is a fixed inherited subspace.

Require an exact embedding map
`E_004_to_005 : Q_RCG004 -> Q_RCG005`
with rank `6` and exact subspace equality for the algebraic cubic sector.

Do not regenerate or replace the RCG004 quotient. Use the persisted canonical RCG004 data as the parent reference.

The embedded RCG004 six-dimensional subspace must reproduce the canonical triaxial higher-derivative obstruction and the old RCG003 shear regression. If it appears second order in the new machinery, classify `INVALID_RCG005`.

## Lower-order reference action

Use
`S = S_EH+Lambda + integral sqrt(|g|) Sum_A c_A I_A`.

EH and `Lambda` are fixed controls only. They are not columns of the RCG005 correction coefficient vector and cannot be used to cancel derivative orders above two.

## Primary geometry — restored lapse, fully triaxial, off shell

Use

`ds^2 = -N(t)^2 dt^2 + exp(2a(t)) dx^2 + exp(2b(t)) dy^2 + exp(2c(t)) dz^2`

with independent arbitrary smooth `N(t),a(t),b(t),c(t)`.

No `N=1` before variation and high-derivative extraction.
No isotropy or axisymmetry.
No equations of motion.
No special trajectory or constant-Hubble assumption.

A non-singular lapse variable such as `n=log N` may be used only if its exact invertible map and derivative transformation are frozen in the implementation before outcome.

## Source-faithful construction

Production curvature and derivative-curvature tensors must be derived from the metric, inverse metric, Christoffels and covariant derivatives. RCG004 compact formulas may be used only as regression controls, not as production definitions for the new derivative sector.

## Exact generalized Euler-Lagrange cascade

For every relevant field `q in {a,b,c,N}` (or prospectively fixed equivalent lapse variable), derive the reduced generalized Euler-Lagrange equation

`E_q = Sum_r (-d/dt)^r [ partial L / partial q^(r) ]`.

Determine the maximum derivative order appearing exactly; do not assume it is six and do not assume odd orders vanish.

For every derivative order strictly above two, collect all independent off-shell jet-monomial coefficients linearly in the RCG005 coefficient vector `C`.

Persist maps `A_m C=0` for each actual order `m>2`, including at minimum explicit checks of orders 6,5,4,3 when present.

Sequential survivor chain:
- `K_max = ker(A_max)`;
- then intersect successively with every lower `m>2` map;
- final `K_SO = intersection_{m>2} ker(A_m)`.

Persist each matrix shape, rank, nullity, kernel basis/span certificate, intersection map and SHA256.

## Critical mechanism discriminator

Report separately whether:
A. the newly added curvature-derivative sector itself has zero kernel already at its highest derivative order;
B. a nonzero derivative-sector highest-order kernel exists but the combined class cannot cancel inherited RCG004 higher derivatives;
C. a nonzero combined direction cancels every `>2` derivative coefficient on the primary geometry.

Do not collapse these mechanisms into a single binary result.

## Independent science routes

Constructor route and direct generalized-Euler route must be implementation-independent where practical. A separate Critic must reconstruct:
- quotient dimension and span;
- exact RCG004 embedding;
- high-derivative maps / rank cascade;
- final `K_SO` span.

Compare survivor spaces via exact mutual-containment ranks, not vector ordering.

Any disagreement in quotient dimension, inherited subspace, derivative-order rank, or survivor span => `INVALID_RCG005` until resolved.

## Frozen controls

Mandatory controls include:
1. canonical RCG004 six-direction regression;
2. duplicate invariant detection;
3. a constructed total derivative reduced to zero action class;
4. violated algebraic/differential Bianchi trap;
5. accidental-isotropy trap;
6. EOM-substitution trap;
7. lapse gauge-fixed-before-variation negative control;
8. basis permutation invariance;
9. coefficient-column permutation invariance;
10. one intentionally omitted raw derivative contraction must be detected as incomplete;
11. one intentionally sign-flipped derivative commutator must be detected;
12. one intentionally wrong 4D identity must be detected;
13. parity-odd contamination rejection;
14. field-redefinition quotient contamination rejection;
15. source lock `VACUUM_ZERO`.

## Held-out rule

At least one independent generic local metric-jet / principal-higher-derivative-symbol gate is frozen separately before primary survivor inspection. If `dim K_SO=0`, held-out survivor testing is `NOT_APPLICABLE_PRIMARY_NULLITY_ZERO`. If `dim K_SO>0`, the held-out acts on the **entire** primary survivor subspace without refit.

## Frozen terminal classifiers

Allowed scientific terminal classes:
- `PASS_SCOPED_RCG005_COMPLETE_LOCAL_DIM6_PURE_METRIC_SECOND_ORDER_SURVIVOR_EXISTS`;
- `FAIL_SCOPED_RCG005_COMPLETE_LOCAL_DIM6_PURE_METRIC_CLASS_HAS_NO_NONZERO_SECOND_ORDER_SURVIVOR`;
- `FAIL_SCOPED_RCG005_PRIMARY_SURVIVORS_FAIL_FROZEN_GENERIC_GEOMETRY_HELDOUT`;
- `INVALID_RCG005`;
- `BLOCKED_RCG005`.

PASS requires valid authority/provenance/completeness, nonzero exact surviving correction subspace, independent agreement and controls; if held-out is applicable it must leave nonzero dimension.

Primary FAIL requires every validity predicate and `dim K_SO=0` exactly.

Geometry-generalization FAIL requires a nonzero primary survivor subspace that is reduced to zero by the frozen held-out without refit.

Infrastructure failure is not scientific FAIL.

## Claim locks

A PASS would not establish a complete theory, ghost freedom, stability, hyperbolicity, unitarity, phenomenology or quantum gravity.

A FAIL would be confined to this prospectively complete local parity-even pure-metric mass-dimension-six class under the frozen exact equivalence and off-shell second-order criterion; it would not prove arbitrary modified-gravity or higher-curvature no-go.

`chi_ABC = UNAUTHORIZED_NOT_COMPUTED`.
`THEORY_ESTABLISHED = 0%`.
