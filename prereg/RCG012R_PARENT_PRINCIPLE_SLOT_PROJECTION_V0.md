# RCG012R — parent-principle slot projection authority — preregistration v0

Status: **PROSPECTIVELY FROZEN BEFORE EXECUTION**

Frontier:
`e3e735500f7a78f4ed6d29a2479d0c3c57c42cea`.

Historical RCG012 is quarantined pre-execution and has no scientific result.

## Frozen question

`DOES_CURRENT_DURABLE_RQIRCG_AUTHORITY_DEFINE_A_NONCOLLAPSING_PROJECTION_FROM_THE_FIVE_MODEL_DEFINITION_SLOTS_TO_THE_NARROWER_PARENT_SOURCE_GENERATING_INFLUENCE_PRINCIPLE_STAGE_SUFFICIENT_TO_AUTHORIZE_A_MINIMAL_CONTENT_BASIS_THEOREM?`

This gate does **not** decide the projection by convenience.

It asks whether the projection already exists in durable authority.

## Frozen five-slot graph

A — `CLASSICAL LAW`

B — `SOURCE / PREPARATION CONSTITUTION`

C — `QUANTUM STATE / MEASURE / BOUNDARY`

D — `QUANTUM LAW / ON-SHELL MATCHING`

E — `OPERATIONAL OBSERVABLE / READOUT`

The graph's non-collapse rules are binding:
- the five slots are logically distinct even when one mathematical package relates them;
- no PASS in one slot grants automatic PASS in another;
- `STATE_MEASURE_SELECTION != QUANTUM_LAW_MATCHING_SELECTION`.

## Required projection object

A valid projection must explicitly assign **every** slot A-E exactly one label:

- `REQUIRED_AT_PARENT_PRINCIPLE_STAGE`
- `DOWNSTREAM_ONLY`
- `NOT_APPLICABLE`

and provide a durable authority reason for each assignment.

It must also define the exact boundary of “parent source/generating/influence principle” so that:
- parent-principle completeness is not confused with successor promotion completeness;
- operational readout is not silently omitted;
- classical law is not silently merged with quantum law;
- quantum law/matching is not silently merged with state/measure.

## Cheap obstruction first

Test:

`P1_EXPLICIT_PROJECTION_RULE_EXISTS`.

If current authority contains no explicit A-E -> parent-stage projection rule, STOP.

Do not infer one from terminology such as “source/state/generating”.

## Requirements

- `P1_EXPLICIT_PROJECTION_RULE_EXISTS`
- `P2_ALL_FIVE_SLOTS_CLASSIFIED_BY_AUTHORITY`
- `P3_NONCOLLAPSE_RULE_PRESERVED`
- `P4_PARENT_STAGE_BOUNDARY_EXPLICIT`
- `P5_OPERATIONAL_READOUT_BOUNDARY_EXPLICIT`
- `P6_CANDIDATE_INDEPENDENCE`
- `P7_OUTCOME_FIREWALL`

## Frozen search controls

In addition to the named exact authority blobs, audit repository code-search for explicit mappings using:

1. `"parent principle" + "Slot A"`
2. `"parent-principle" + "OPERATIONAL MAP"`
3. `"five slots" + "parent"`
4. `"projection" + "model-definition slot"`

Search hits are navigation controls only. A PASS requires exact durable content, not search text alone.

## Terminal taxonomy

Exactly one:

1. `PASS_SCOPED_RCG012R_PARENT_PRINCIPLE_SLOT_PROJECTION_DEFINED`
2. `BLOCKED_SCOPED_RCG012R_NO_AUTHORIZED_PROJECTION_FROM_FIVE_SLOT_GRAPH_TO_PARENT_PRINCIPLE`
3. `BLOCKED_SCOPED_RCG012R_PARENT_STAGE_BOUNDARY_AMBIGUOUS`
4. `INVALID_RCG012R_IMPLEMENTATION_OR_PROVENANCE`

## Interpretation ceiling

A BLOCKED result means only that the current repository does not yet authorize a unique stage projection. It does not prove that no sensible projection can be defined.

A PASS would authorize a later minimal-basis gate; it would not itself define a parent principle.

## Locks

Unchanged:
`A=span{alpha}`; selector rank 0; residual dimension 1;
parent principle/function/source object undefined;
no value/bridge/alpha-sensitivity;
`alpha=UNSELECTED`;
`chi_ABC=UNAUTHORIZED_NOT_COMPUTED`;
`THEORY_ESTABLISHED=0%`.
