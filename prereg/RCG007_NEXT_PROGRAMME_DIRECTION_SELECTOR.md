# RCG-007 — next programme direction selector (PREOUTCOME)

Date: 2026-09-17
Status: **PROSPECTIVELY FROZEN AFTER TERMINAL RCG006/RCG006C AND BEFORE ANY RCG007 SCIENTIFIC OUTCOME**

Parent scientific terminal:
`results/RCG006_FIELD_REDEFINITION_EQUIVALENCE_AUDIT_TERMINAL.md`, commit `e3a567d37911c1d86c5a98e3e9febc4938384731`.

Independent identity bridge:
`results/RCG006C_UNIQUE_EFT_CLASS_WEYL_CUBED_TERMINAL.md`, commit `d072cabcc4f5ccf46af382e16b7dbe62c935f3c4`.

Frozen parent facts relevant to programme selection:
- complete RCG005 v0 bulk action quotient dimension `8`;
- first-order local metric field-redefinition image rank `7`;
- unique bulk EFT quotient dimension `1`;
- that unique quotient class is exactly parity-even `Weyl^3`;
- the RCG005 representative-level higher-derivative discriminator is non-invariant along redundant EFT orbits;
- quotient-aware second-order survivor dimension remains `0`;
- historical G88–G91 already established that inherited lower-order/CTP/covariant constraints can have selection rank zero on higher-order completion coefficients and that bare covariance plus a frozen lower perturbative jet is not a sufficient nonlinear selector in the audited families.

No coefficient for `Weyl^3` has been selected or inferred.

## Why a new selector is required

RCG006 closes an equivalence-class ambiguity but does not form a new candidate theory. RCG006C identifies the one nonredundant local parity-even pure-metric class at the frozen dimension, but identity of an operator class is not authority to choose its coefficient or to append higher-order/new-field structure.

Proceeding directly to `R^4`, adding a field, picking a nonlocal kernel, or assigning a `Weyl^3` coefficient would be post-outcome theory patching unless one bounded direction is selected prospectively.

This selector is governance only. It computes no RCG007 scientific result.

## Frozen candidate directions

A programme operator may choose exactly one disposition.

### A — inherited-RQIR selection-rank audit of the unique Weyl-cubed coefficient

`SELECT_RCG007_WEYL3_INHERITED_RQIR_SELECTION_RANK_AUDIT`

No new model family is formed. Freeze one coefficient `alpha` multiplying the already-identified nonredundant parity-even `Weyl^3` EFT class and ask whether the **already-frozen, candidate-independent RQIR-owned constraints and already-authorized observable/interface objects** have nonzero exact selection rank on `alpha`.

This route may determine only structural dependence/independence and exact selection rank. It may not invent a new observable, tune `alpha`, or compute `chi_ABC` without separate authority.

Possible terminal outcomes include selection rank zero (precise blocker), nonzero rank with a separately authorized coefficient-solving successor, falsification of the class under an inherited constraint, or object-definition block.

### B — next local pure-metric EFT class

`SELECT_RCG007_LOCAL_DIM8_PURE_METRIC_EFT_CLASS`

Authorize at most one prospectively frozen mechanically complete local parity-even pure-metric engineering-dimension-eight action/EFT quotient audit, including field-redefinition equivalence from the start. No post-hoc operator subset may define the family.

### C — additional dynamical field architecture

`SELECT_RCG007_ADDITIONAL_FIELD_BOUNDED_CLASS`

Authorize one finite field-content architecture with explicit spin/kinetic/coupling bounds frozen before scientific outcome. No field may be added after seeing failures.

### D — bounded nonlocal architecture

`SELECT_RCG007_NONLOCAL_BOUNDED_CLASS`

Authorize one finite falsifiable kernel/form-factor architecture. Arbitrary functions and outcome-conditioned functional freedom are forbidden.

### E — boundary/matter-equivalence structural audit

`SELECT_RCG007_BOUNDARY_OR_MATTER_EQUIVALENCE_AUDIT`

No new gravity family is formed. Test one prospectively bounded part of the RCG006 interpretation ceiling — boundary observables or matter-coupling transformation — before model expansion. The exact subobject must be frozen separately after selection.

### H — hold

`HOLD_RCG007_PROGRAMME`

No new scientific gate is opened.

No implicit/default selection is allowed.

## Frozen programme priorities

The external programme operator must use the following lexicographic priorities, in order; a later priority cannot override an earlier one merely because a route looks more likely to produce a survivor.

1. **Fatal upstream dependency:** prefer a gate whose result determines whether existing frozen information can already select/falsify the current unique nonredundant class before any new degrees of freedom are introduced.
2. **Minimum added freedom:** structural audits of already-existing objects outrank formation of larger model classes when they address the same programme blocker.
3. **Direct RQIR relevance:** prefer tests that connect candidate gravity back to frozen RQIR requirements/observable/interface constraints over neighboring higher-curvature algebra alone.
4. **Whole-class falsifiability and exactness:** prefer exact rank/object-identity gates with bounded completeness over broad numerical exploration.
5. **Downstream unlocks per cost:** prefer a result that either authorizes a sharply defined next step or produces a terminal blocker explaining what datum is missing.
6. **Anti-rescue:** never choose a route because it is expected to preserve a nonzero survivor.

Under these priorities, direction A has a structural advantage over B/C/D if the already-frozen RQIR objects needed to define the selection-rank audit actually exist. If those objects cannot be identified exactly without inventing a source/interface convention, A must terminally block on object definition rather than silently importing one; that blocked result itself then informs the next selector.

## Required declaration content

A valid declaration after this selector must contain:
- acting programme authority/process;
- exactly one disposition above;
- statement that selection is governance, not scientific evidence;
- preservation of all RCG003B–RCG006 terminals;
- preservation of independent-construction firewall from QGR/MSQGR/KMQGB dynamics;
- prohibition on post-hoc coefficient/operator/field/kernel changes;
- authorization for exactly one bounded successor scientific preregistration;
- preservation of `chi_ABC = UNAUTHORIZED_NOT_COMPUTED` and `THEORY_ESTABLISHED = 0%`.

## Frozen classifier

- `RCG007_PROGRAMME_DIRECTION_SELECTED_SCOPED` after one valid explicit non-HOLD declaration;
- `RCG007_PROGRAMME_HELD_SCOPED` after valid HOLD;
- `BLOCKED_PENDING_EXPLICIT_RCG007_PROGRAMME_DIRECTION` before a valid declaration;
- `INVALID_RCG007_SELECTION` for ambiguous, multi-choice, retroactive, outcome-conditioned, or rescue-motivated selection.

## Anti-patching locks

Before a valid selection is incorporated, do not:
- assign or fit a `Weyl^3` coefficient;
- form a dimension-eight basis;
- add a field;
- choose a nonlocal kernel;
- invent a new RQIR observable/source/interface datum;
- reinterpret historical RCG005/RCG006 terminals;
- compute `chi_ABC`.

No current result establishes new physics, a preferred modified-gravity theory, quantum gravity, stability, causality, unitarity or phenomenological viability.

`chi_ABC = UNAUTHORIZED_NOT_COMPUTED`.
`THEORY_ESTABLISHED = 0%`.
