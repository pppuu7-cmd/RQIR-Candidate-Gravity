# RCG-002 / RSC-MAPF1 — minimal multi-axiom package formation — PREOUTCOME

Status: **PREREGISTERED / NO CONNECTED OUTCOME AUTHORIZED**

Starting authority: `70b180b237057b182a00c7cca11a95383cf7c775`.

## Purpose

SCPT1 and QCPT1 independently established that current RCG-002/RSC authority selects neither a physical source constitution nor a physical quantum state/measure/influence constitution. The next admissible question is not to fit a connected coefficient. It is whether one minimal, prospectively motivated new-version package can jointly close both missing interfaces in the same realization.

This gate is package formation only. It must not compute `chi_ABC`, connected phase/noise outcomes, novelty observables, or use any desired connected result as a selector.

## Target hypothesis

`H_MAPF1`: the strongest independently motivated RSC clauses can be assembled into one complete model-definition package whose new axioms are fixed before outcomes and whose mathematical objects are sufficiently explicit to authorize a future derivation/falsification gate.

The gate is allowed to find only a package **schema** rather than a physical package. A schema with unresolved model-defining slots is not a PASS.

## Frozen package skeleton — CRVQ

The only package assembled in this gate is the **Closed Relational Variational-Quantum (CRVQ) skeleton**, built from principle-level clauses already motivated by the RCG-002 programme:

A1. **Closed realization completeness.** Probe, apparatus/support/control/binding and carrier degrees participating in a realization belong to one closed model; probe-only source bookkeeping is forbidden.

A2. **Unified variational source/self-source.** The local total source and carrier self-source must be derived from the same prospectively specified action/constraint constitution; no post-hoc stress representative is allowed.

A3. **Minimal local covariance.** The package must be local/causal in its declared domain, introduce no new independent dimensional scale solely to rescue the nonlinear layer, and recover the inherited linearized spin-2 carrier/source law. Any rule eliminating curvature/nonminimal couplings must be stated in a fixed physical field representation rather than by the word `minimal` alone.

A4. **Complete quantum preparation constitution.** The physical initial state/measure/boundary data of all unobserved carrier/environment degrees must be specified by an exact prospective rule, not merely by saying `vacuum`, `pure`, `closed`, `least noisy` or `prepared`.

A5. **Derived operational influence.** The normalized positive operational map must be derived from the same closed dynamics and state/measure by a specified unitary/CTP/reduction prescription. Positivity/CPTP may be a theorem or a frozen test route, but no arbitrary higher connected phase/noise functional may be appended.

A6. **Exact inherited recovery.** G97 closed-preparation bookkeeping and the validated weak-field/pairwise RCG-002 baseline must be recovered in their exact scopes.

A7. **Independence.** No dynamics, preferred coefficient, action, state or conclusion may be imported from QGR, MSQGR/CRQN, KMQGB, RQIR, ISQGR or another candidate project. Standard mathematics and standard GR/QFT/EFT facts may be used only as explicitly labelled background/calibration, not as selectors.

## Mandatory formation slots

A CRVQ package counts as formed only if all of the following are populated by explicit mathematical objects or unique prospective rules:

S1. physical probe degrees;
S2. apparatus/support/control/binding degrees needed for closed preparation;
S3. source/action domain and physical field representation;
S4. allowed local curvature/nonminimal operators and a rule fixing every associated coefficient;
S5. local total source and carrier nonlinear self-source/action relation;
S6. conservation/constraint/Bianchi propagation route;
S7. complete physical quantum state/measure/boundary prescription for unobserved degrees;
S8. exact unitary/CTP/reduction prescription yielding normalized positive operational evolution;
S9. exact weak-field/pairwise/G97 recovery route;
S10. no free connected phase/noise functional or hidden selector objective.

Placeholders such as `choose a suitable action`, `take the vacuum`, `use minimal coupling`, `trace the environment`, `pick the simplest state` or `use the least noisy extension` do not populate a slot unless the relevant object and selection rule are mathematically fixed.

## Independent-motivation rule

Every genuinely new axiom must satisfy both:

M1. it is motivated by an already-authoritative RCG-002 consistency/operational requirement or by a terminal blocker, before any connected outcome;

M2. its exact content is not selected merely because it would remove an ambiguity, reproduce a desired connected signal, or resemble a successful external candidate.

A rule may be motivated at principle level yet still fail package formation if its exact mathematical content remains underdefined.

## Frozen source-side false-positive calibration

Use only as a mathematical calibration, not as RCG-002 matter physics:

`S_xi[g,phi] = Integral sqrt(-g) [ -1/2 g^{mu nu} partial_mu phi partial_nu phi - V(phi) - 1/2 xi R phi^2 ] d^4x`.

The gate must test whether the CRVQ clauses actually fix the dimensionless `xi`-type ambiguity once the physical source representation is specified. If the package merely says `one action`, `covariant`, `two derivative`, `no new scale` or `minimal` without a basis/ontology rule, this calibration counts against formation.

No physical RCG-002 completion coordinate may be inferred from `xi`.

## Frozen quantum-side false-positive calibration

For fixed controlled microscopic dynamics

`U = sum_x |x><x| tensor U_x`,

and an unobserved initial state `rho_E`, the reduced multiplier is

`K_rho(x,y)=Tr[rho_E U_y^dagger U_x]`.

The gate must test whether A4 supplies an exact physical state/measure rule rather than only closed/unitary dynamics.

Use the explicit same-dynamics control

`U_0=I`, `U_1=Z`

with pure states `rho_0=|0><0|` and `rho_plus=|+><+|` if needed. Both produce normalized positive kernels but different operational maps.

A stronger lower-domain control is also frozen: if a history selector `q_x` vanishes on every already-authorized lower-order domain, take `U_x=exp(i alpha q_x Z)` with fixed nonzero `alpha`. Then all lower-domain dynamics are identical (`U_x=I`) while the higher-history reduced map can remain state-dependent. No connected observable may be evaluated from this control.

No physical RCG-002 completion coordinate may be inferred from the state labels or `alpha`.

## Frozen combined ambiguity test

Source/action and quantum-state calibrations are independent. The gate may form their Cartesian product only to test whether a proposed package leaves two logically independent model-definition slots open.

A two-parameter synthetic calibration family is **not** evidence that the physical gravitational completion space has dimension two. Physical selector rank remains `UNDEFINED_PHYSICAL_MAP_MISSING` unless a physical map is actually defined.

## Positive structural control

A genuinely complete toy model with explicitly fixed closed degrees, exact action/couplings, exact physical state, exact reduction map, proven normalization/positivity, causal domain and inherited lower-order recovery would satisfy the structural definition of `PACKAGE_FORMED_PREOUTCOME`. Such a toy would still not automatically be RCG-002 physics unless its axioms satisfy M1-M2.

## Frozen decision rule

### PASS

`RSC_MINIMAL_MULTI_AXIOM_PACKAGE_FORMED_PREOUTCOME`

Only if S1-S10 are all populated and every new axiom satisfies M1-M2. PASS authorizes only a new-version derivation/falsification preregistration. It does not authorize a connected prediction.

### BLOCKED

`RSC_PACKAGE_SCHEMA_ONLY_MODEL_DEFINITION_INCOMPLETE`

if CRVQ is coherent as a schema but at least one mandatory model-defining slot remains a placeholder or has multiple unresolved prospective choices.

### FAIL

`RSC_PROPOSED_MULTI_AXIOM_PACKAGE_INTERNALLY_INCONSISTENT_SCOPED`

only if the explicit frozen package contradicts one of its own mandatory requirements or an inherited exact authority.

### INVALID

`INVALID_POSTHOC_OR_EXTERNAL_SELECTOR`

if any action/state/coupling/source/kernel choice is selected using a connected outcome, imported candidate dynamics, or altered criteria after inspection.

## Interpretation ceiling

This gate can decide only whether a minimally specified **new-version package** has been formed prospectively. It cannot establish uniqueness of all possible future RSC packages, impossibility of RSC, GR, a nonlinear quantum-gravity theory, a physical completion-space dimension, a connected prediction, new physics, or full quantum gravity.

Programme readiness remains 66%; theory established remains 0% unless a separate authority changes them.

`chi_ABC` remains unauthorized.
