# RCG-002 / RSC-CSA1 — closed-source action proposal pre-outcome gate

Status: `PREREGISTERED / NO CONNECTED OUTCOME AUTHORIZED`
Date: 2026-09-14
Starting authority: `063d3a203e370e417fc5325aa1d12f557fea35b3`

## HYPOTHESIS

Test whether the current RCG-002 relational/operational commitments are sufficient to prospectively select one closed-system source/action constitution for the RSC line, rather than merely require that some conserved stress tensor exist.

The target is not a connected phase and not a fitted coefficient. The target is the missing model-defining source object itself.

## CANDIDATE OBJECT

A qualifying source/action package must specify, in one realization and before any connected outcome:

1. the physical probe degrees of freedom;
2. apparatus/support/control/binding degrees required by the G97 closed preparation;
3. branch/history preparation variables;
4. one closed source action or equivalent constitutive object;
5. a local symmetric conserved total stress/source object derived from that constitution;
6. the carrier nonlinear self-source/action rule in a compatible variational/constraint framework;
7. conservation/constraint propagation in the same realization;
8. exact reduction to the validated linearized weak-field baseline;
9. no imported external-candidate dynamics or preferred coefficient;
10. no free connected coefficient/function chosen from the desired `chi_ABC`, noise, novelty or holdout outcome.

## NEW INFORMATION SOUGHT

SSE1 established that:

- G97 global closure does not select a unique local stress;
- charge equality plus local conservation does not imply source equivalence;
- the repository contains no complete probe+apparatus/support action or carrier self-source rule.

CSA1 asks a stricter question: does RCG-002 already motivate a specific constitutive principle that removes that ambiguity, or would any such choice introduce genuinely new model data?

## FROZEN AUTHORITY SET

Use only the following RQIRCG authority plus general mathematical identities:

- `candidates/RCG_002_RELATIONAL_CONTROLLED_PHASE_CHANNEL.md`;
- `docs/CONSTRUCTION_CONTRACT.md`;
- `candidates/RCG_002_COVARIANT_BASELINE_COMPLETION_V0.md`;
- `results/ITER095_G97_CLOSED_TOTAL_SOURCE_PREPARATION_TERMINAL.md`;
- `results/RCG002_RSC1_INTERFACE_CLOSURE_PREOUTCOME_TERMINAL.md`;
- `results/RCG002_RSC_SSE1_SOURCE_STRESS_EQUIVALENCE_TERMINAL.md`;
- `recovery/CURRENT_FRONT.md` at the starting authority.

No QGR/MSQGR/CRQN/KMQGB/RQIR/ISQGR candidate dynamics or preferred coefficient may be imported.

## PROSPECTIVE PRINCIPLE UNDER TEST

Freeze one minimal generic candidate-owned framework, called `CVS` — Closed Variational Source:

> The physical source of the RSC carrier is the metric/geometry variation of the same closed action (or exactly equivalent constitutive functional) that generates the probe+apparatus/support preparation. The source may not be chosen independently of the preparation. Any carrier self-source must arise from the same prospectively specified variational/constraint constitution. No connected coefficient may be inserted independently.

CSA1 does **not** assume that this framework is sufficient. It tests whether current RCG-002 commitments specify the actual action/constitution strongly enough for CVS to become a physical source law.

## ACTION-LEVEL FALSE-POSITIVE CALIBRATION

Before inspecting any CSA1 verdict, freeze the following synthetic calibration family. It is **not** candidate physics and must not be promoted to an RCG-002 matter ontology.

For a generic scalar calibration degree of freedom `phi`, define

`S_xi[g,phi] = Integral sqrt(-g) [ -1/2 g^{mu nu} partial_mu phi partial_nu phi - V(phi) - 1/2 xi R phi^2 ] d^4x`.

The parameter `xi` is varied only to test whether the generic rule “derive stress by metric variation of a covariant action” is by itself a unique source selector.

Frozen checks:

1. on exactly flat `g=eta`, `R=0`, so the matter equations obtained from the displayed family are independent of `xi`;
2. the flat-space Hilbert stresses may differ by an identically conserved improvement term;
3. if this occurs, the family is a false-positive control showing that `metric variation + flat preparation data` does not by itself select a unique local gravitational source;
4. this calibration does **not** prove that the actual RCG-002 probe/apparatus constitution is a scalar field or contains the same nonminimal term.

## POSITIVE CONTROL

A hypothetical fully specified closed action `S_closed[g,P,A,C]` with all probe/apparatus/support/control variables identified, all coefficients fixed prospectively, and a compatible carrier action/self-source would satisfy the object-definition part of the gate if its derived stress is unique modulo a separately proven physical equivalence.

## NEGATIVE CONTROLS

The following do not qualify:

- probe-only stress with apparatus/support omitted;
- an externally held source with uncompensated external force;
- “choose the minimal stress tensor” without a candidate-owned rule selecting that action/representative;
- a conserved improvement quotient justified only by equality of global charges;
- importing Einstein-Hilbert/standard GR nonlinear dynamics as the RSC selector;
- a preferred pseudotensor chosen post hoc;
- an action term chosen because it produces a convenient/nonzero/large/small connected phase;
- any free curvature-coupling, self-coupling or constitutive function not fixed before outcome.

## PASS

`RSC_CLOSED_SOURCE_ACTION_PRINCIPLE_SELECTED_PREOUTCOME` only if all of the following hold:

1. a specific closed source/action constitution is identified from RCG-002 commitments rather than imported dynamics;
2. all physical source degrees required by G97 closure are included;
3. the local total source is derived rather than independently fitted;
4. source-representative freedom is fixed or quotiented by a proven carrier+readout invariance;
5. the carrier self-source rule is specified without a new free nonlinear coefficient/function;
6. exact weak-field recovery is structurally available;
7. no connected outcome is used in selection.

PASS would authorize only a new-version source-side definition gate. It would not authorize `chi_ABC`, quantum positivity, new physics or full RSC.

## FAIL

`CURRENT_RCG002_COMMITMENTS_DO_NOT_SELECT_CLOSED_SOURCE_ACTION_SCOPED` if an explicit prospective countermodel/calibration demonstrates that the same current operational/preparation commitments admit inequivalent action-derived local source laws, and no current candidate-owned criterion selects among them.

This is a scoped non-entailment result, not a theorem that no future RCG-002 source constitution can exist.

## BLOCKED

`BLOCKED_SOURCE_ACTION_OBJECT_UNDERDEFINED` if the repository lacks enough physical degrees/constitution to even formulate a unique candidate action or test the required source/self-source map.

## INVALID

`INVALID_CSA1_CONTRACT_VIOLATION` if any connected phase/noise outcome, desired novelty property, external-candidate dynamics, or post-hoc source choice is used to select the action.

## INTERPRETATION CEILING

CSA1 can establish only whether current RCG-002 commitments prospectively select a closed source/action constitution, or whether new constitutive information is still required.

It cannot establish:

- a unique gravitational theory;
- full nonlinear diffeomorphism/Bianchi closure;
- quantum state/measure/influence positivity;
- a physical connected phase;
- new physics;
- full quantum gravity.

## CLAIM LOCKS

Retain:

- `theory established = 0%`;
- programme readiness = 66% unless separate authority changes it;
- physical selector rank = `UNDEFINED_PHYSICAL_MAP_MISSING` unless a physical map is actually defined;
- physical nonlinear completion space = `UNDEFINED`;
- no `chi_ABC` calculation before this source/action gate terminalizes;
- `BLOCKED != FAIL`;
- finite-family/calibration result != universal theorem.
