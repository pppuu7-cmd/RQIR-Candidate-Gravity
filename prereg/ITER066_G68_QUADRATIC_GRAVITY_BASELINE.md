# Iter066 / G68 — standard quadratic-gravity baseline equivalence / novelty audit (FROZEN)

Prospectively preregistered on 2026-09-13 before G68 implementation or production output.

## Question
Is the complete G60 two-dimensional local four-derivative escape space, together with the G61–G67 pole/projector/source-saturation structure derived from it, exactly the standard local curvature-squared quadratic-gravity baseline in four dimensions (modulo the Gauss-Bonnet direction), rather than a candidate-specific RCG-002 deformation?

A supported equivalence means only that **novelty is not established in this local four-derivative layer**. It does not falsify RCG-002 globally and does not exclude candidate-owned nonlocal, higher-derivative, nonlinear, state-dependent or quantum-measure structures.

## Frozen external baseline anchors
These references are fixed before implementation and used only to classify known theory space:
1. K. S. Stelle, *Renormalization of higher-derivative quantum gravity*, Phys. Rev. D 16, 953 (1977), DOI `10.1103/PhysRevD.16.953`.
2. A. Salvio, *Quadratic Gravity*, Front. Phys. 6, 77 (2018), DOI `10.3389/fphy.2018.00077`.

Frozen standard local curvature-squared action layer:
`Delta L = gamma Riemann^2 + beta Ricci^2 + alpha R^2`.
In four dimensions, quotient by the Gauss-Bonnet direction
`GB = Riemann^2 - 4 Ricci^2 + R^2`.
The frozen G60 basis is `a R^2 + b Ricci^2`.

## Held-out algebra panel
Use exact rational/symbolic arithmetic and momenta not used as the primary G60 rank panel:
`(2,3,1,5),(4,1,3,2),(5,2,4,1),(3,4,1,2),(2,5,1,4)`.
Held-out curvature coefficient triples `(gamma,beta,alpha)`:
`(1,2,3),(-2,5,1),(3,-4,2),(5,1,-3),(-3,-2,7),(2,0,-5)`.
Frozen effective quotient map:
`b = beta + 4 gamma`, `a = alpha - gamma`.

Frozen Weyl-squared relation in 4D:
`C^2 = Riemann^2 - 2 Ricci^2 + R^2/3`, hence modulo GB,
`C^2 = 2 Ricci^2 - (2/3) R^2`.
Therefore the equivalent `(R^2,C^2)` coordinates of the G60 pair are
`lambda_C=b/2`, `lambda_R=a+b/3`.

## Stream A — independent Gauss-Bonnet quotient reconstruction
On every held-out momentum, independently construct the exact quadratic Hessians of `Riemann^2`, `Ricci^2`, `R^2` around flat space. Require:
- exact rank 2 of the invariant column space;
- unique projective null direction `(1,-4,1)`;
- no second independent null direction.

## Stream B — exact general-curvature coefficient quotient map
For every held-out `(gamma,beta,alpha)` and momentum:
- require exact equality of the quadratic form generated by `gamma Riemann^2 + beta Ricci^2 + alpha R^2` and by `b Ricci^2 + a R^2` with frozen `b=beta+4gamma`, `a=alpha-gamma`;
- require invariance under at least four nonzero exact shifts along the Gauss-Bonnet coefficient direction `(gamma,beta,alpha)->(gamma+t,beta-4t,alpha+t)`.

## Stream C — independent `(R^2,C^2)` basis equivalence
For every frozen G60 coefficient ray and held-out momentum:
- construct `C^2` directly from `Riemann^2-2 Ricci^2+R^2/3`;
- require exact equality between `a R^2+b Ricci^2` and `lambda_R R^2+lambda_C C^2` modulo the frozen GB identity, using `lambda_C=b/2`, `lambda_R=a+b/3`;
- require the coordinate transformation to have exact nonzero determinant / rank 2.

## Stream D — sector and source-response reproduction
Independently derive the TT/scalar quadratic sector polynomials from `EH + a R^2 + b Ricci^2` and require exact reproduction of the G61 forms
`P_TT=z*(-2+b*z/2)` and `P_S=z*(6+3*(3a+b)*z)`.
Then on the frozen G65 conserved-source panel require exact reproduction of the G67 source-saturated direct/partial-fraction response at held-out nonsingular `z` values. This stream must not import precomputed G61/G67 output values beyond the frozen target formulas.

## Stream E — scope/false-equivalence controls
Require all controls:
1. a pure Gauss-Bonnet shift does not change the quadratic response;
2. duplicating the same physical basis direction cannot fake rank 2;
3. an explicit six-derivative polynomial deformation containing a nonzero `z^3` term is detected as outside the curvature-squared (`z^2` maximum) baseline;
4. an explicit rational/nonlocal response term `z^2/(1+z)` is detected as outside the local curvature-squared polynomial class;
5. the audit classification is restricted to the G60–G67 local linearized layer and cannot label the entire RCG-002 programme baseline-equivalent.

## Aggregate rule
PASS iff A/B/C/D/E are all structural-valid and pass.

Allowed PASS classification only:
`G60_G67_LOCAL_FOUR_DERIVATIVE_ESCAPE_BASELINE_EQUIVALENT_TO_STANDARD_QUADRATIC_GRAVITY_SCOPED`.

A valid frozen-predicate violation is `SCIENTIFIC_FAIL_G68_<streams>`. Missing/malformed/duplicate artifacts are `INFRASTRUCTURE_OR_ARTIFACT_INVALID`.

## Claim ceiling
A PASS means the already-derived **local linearized four-derivative** G60–G67 branch lies inside the standard quadratic-gravity baseline and therefore does not, by itself, establish RCG-002 novelty. It does not prove that RCG-002 is globally standard, false, inconsistent or non-quantum-gravitational. It does not establish a physical ghost or quantum nonunitarity.

Programme readiness remains **66%** and theory established remains **0%** regardless of G68 PASS/FAIL unless a separately preregistered rubric authorizes a change.
