# Iter057 / G59 — prospective preregistration

Gate: `TWO_DERIVATIVE_LINEARIZED_CONSTITUTION_FREEDOM_AUDIT`
Date frozen: 2026-09-13
Status at freeze: NO production evidence exists for this gate.
Readiness credit: 0 by itself; programme readiness remains capped at 66%.

## Scientific question
Inside the sharply scoped class of a single symmetric rank-2 field on 4D Minkowski spacetime, local Lorentz-invariant quadratic actions with exactly two derivatives and linear gauge transformation
`delta h_{mu nu} = partial_mu xi_nu + partial_nu xi_mu`, determine whether gauge/conservation consistency leaves any nontrivial deformation freedom beyond one overall normalization.

This is a candidate-owned structural audit. It imports no physical ansatz or result from QGR/KMQGB/RQIR. A uniqueness result is only scoped to the frozen assumptions and is not a no-go theorem for higher derivatives, nonlocality, extra fields, nonlinear completions, semiclassical models, or quantum gravity.

## Frozen action basis
Use
`L = a (d_l h_mn)(d^l h^mn) + b (d_m h^mn)(d^l h_ln) + c (d_m h)(d_n h^mn) + d (d_l h)(d^l h)`
up to total derivatives, with coefficient vector `q=(a,b,c,d)`.

The canonical reference ray for the above basis is frozen as proportional to
`q_FP = (1,-2,2,-1)`.
Only proportionality is tested; no normalization is fitted to data.

## Independent streams
A. **Exact gauge-nullspace derivation.** Build the momentum-space quadratic form over exact rational samples of non-null 4-momenta and gauge vectors. Construct the linear constraint matrix for invariance under pure-gauge directions. Required: rank=3, nullity=1, exact null ray proportional to `(1,-2,2,-1)`.

B. **Held-out gauge/Bianchi transfer.** On disjoint exact rational momenta/tensors not used in A, evaluate the Euler operator generated by the canonical ray. Required: contraction with each pure-gauge mode is exactly zero and `p_mu E^{mu nu}=0` exactly. Wrong-ray control `(1,-2,2,-0.8)` must violate at least one held-out identity.

C. **Conserved-source exchange structure.** For disjoint numerical conserved symmetric sources and spacelike momenta, compare the gauge-fixed exchange numerator to the 4D massless-spin-2 conserved-source structure proportional to `T_mn T^mn - 1/2 T^2`, after eliminating one common normalization using one calibration-free ratio. Required relative residual <=1e-10 over >=8 held-out source pairs. Wrong trace coefficient `-1/3` must exceed relative residual 1e-4 on >=6/8 cases. This is a structural control, not an experimental fit.

D. **Basis/sample robustness.** Repeat A under at least 4 invertible coefficient-basis transformations and >=3 independently generated rational sample batches. Required nullity remains 1 and transformed null ray maps back exactly to the same projective ray. Include one deliberately rank-deficient sample batch as a positive diagnostic control; it must be detected as insufficient rather than promoted.

## Frozen classification rule
- `TWO_DERIVATIVE_LINEARIZED_LOCAL_CONSTITUTION_UNIQUE_UP_TO_NORMALIZATION_SCOPED` iff A/B/C/D all valid and pass.
- `NONTRIVIAL_TWO_DERIVATIVE_FREEDOM_DETECTED_SCOPED` only if A and D validly establish nullity >1 with held-out B consistency; this outcome must be preserved, not repaired away.
- `G59_FROZEN_RULE_NOT_MET` for a valid scientific failure of any required predicate.
- `INFRASTRUCTURE_OR_IMPLEMENTATION_INVALID` only for technical/control invalidity that prevents applying the frozen rule.

## Claim ceiling
Even a full PASS means only: under the frozen single-field/local/Lorentz-invariant/quadratic/two-derivative/linear-gauge assumptions, the action ray is unique up to normalization and the current weak-field candidate has no deformation freedom inside that class. It does NOT establish nonlinear GR, full diffeomorphism invariance, quantization, unitarity, experimental validity, or a general no-go theorem.

No thresholds, basis family, source family, canonical ray, or interpretation rule may be weakened after production evidence is observed.
