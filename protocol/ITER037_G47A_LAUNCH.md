# Iter037 / G47-A production launch

Authorized only after terminal G47-C classification `THREE_STATE_CLASSICAL_SWITCHING_OPTIMIZER_CALIBRATED`.

Launch the unchanged 8-lane adversarial matrix defined in `protocol/ITER037_G47A_THREE_STATE_SWITCHING_ADVERSARIAL.md`. Preserve the calibrated 12D family/bounds/QMC/optimizer and the exact G44-A RCG-002 target convention. Frozen gap and cross-method agreement thresholds must not be changed after production results are visible.
