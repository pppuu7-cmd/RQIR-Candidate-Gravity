# RCG-007 — external programme selection declaration

Date: 2026-09-17
Status: **EXPLICIT GOVERNANCE DECLARATION AFTER SELECTOR FREEZE**

Frozen selector:
`prereg/RCG007_NEXT_PROGRAMME_DIRECTION_SELECTOR.md`, commit `1b529a2b2debd7eb71755e6b8099ea7369f5c15c`.

Acting programme authority/process:
`EXTERNAL_RQIRCG_PROGRAMME_OPERATOR_ISSUING_CONTINUE_MAXIMALLY_EFFECTIVE_INSTRUCTION`.

The current programme instruction authorizes continuing RQIRCG with maximum scientific efficiency. Under the lexicographic priorities prospectively frozen in the selector, the explicit disposition is:

**`SELECT_RCG007_WEYL3_INHERITED_RQIR_SELECTION_RANK_AUDIT`**.

## Why this disposition wins the frozen priorities

1. RCG006/RCG006C leave exactly one nonredundant local parity-even pure-metric bulk EFT class, `Weyl^3`, with no selected coefficient.
2. Before adding any new degree of freedom, the fatal upstream question is whether the already-frozen RQIR information can select or falsify that coefficient at all.
3. This route adds zero model freedom and connects the gravity construction directly back to the repository's stated RQIR requirements → dynamics → observable-map path.
4. Exact selection rank is a bounded falsifiable object: rank zero is a scientifically useful terminal blocker, not a reason to rescue the class.
5. Dimension-eight, new-field and nonlocal routes remain available only after this minimal-freedom question is resolved or blocks on a genuinely missing datum.

This rationale is governance, not evidence that the RCG007 scientific gate will pass or fail.

## Scope authorized

Authorize exactly one prospectively controlled structural audit of whether the already-existing, already-frozen RQIR-owned constraints/authorized observable or interface objects have nonzero exact selection rank on a single symbolic coefficient `alpha` multiplying the terminal RCG006C `Weyl^3` quotient class.

The successor audit must:
- identify every consumed RQIR object by exact repository authority before evaluating `alpha` dependence;
- distinguish inherited information from newly introduced information;
- permit `BLOCKED_MISSING_DATUM` if the required object is not actually frozen;
- forbid fitting or assigning `alpha`;
- forbid inventing a new source, local stress representative, carrier self-source, influence completion, boundary map or observable;
- forbid `chi_ABC` computation;
- preserve all historical RCG003B–RCG006 results and the RCG006C identity bridge;
- preserve the independent-construction firewall from QGR/MSQGR/KMQGB candidate dynamics.

## Not authorized

This declaration does not authorize:
- dimension-eight family formation;
- additional fields;
- nonlocal kernels;
- boundary/matter-equivalence generalization;
- a numerical or analytic value for `alpha`;
- post-hoc observable creation;
- `chi_ABC`;
- a theory-established or new-physics claim.

`chi_ABC = UNAUTHORIZED_NOT_COMPUTED`.
`THEORY_ESTABLISHED = 0%`.
