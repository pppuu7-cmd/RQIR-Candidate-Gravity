# RCG-002 / RSC-SCPT1 terminal — source constitution principle triage pre-outcome

Final classification: `NO_PREOUTCOME_SOURCE_CONSTITUTION_SELECTED`

Secondary result: `RSC_SOURCE_SIDE_REQUIRES_NEW_MODEL_DEFINING_ONTOLOGY_SCOPED`

RSC status: `NEAR_SURVIVOR_NOT_SELECTED`

Starting authority: `c48a3b4dda6bb327092147272beed40a0d462d3a`
Preregistration: `8391ff875957fb2cece7e46eb4771db87295671f`

No `chi_ABC`, connected phase/noise, novelty observable, preferred nonlinear coefficient, or held-out connected outcome was computed or used.

## Frozen proposal set

P1 `MGC — Minimal Geometric Coupling`

P2 `GML — G97 Mechanical Lift`

P3 `OSQ — Operational Source Quotient`

P4 `UVC — Unified Variational Closure`

Mandatory survivor criteria were frozen before evaluation in the preregistration.

## P1 — MGC

### Assessment

C1 RCG002 grounding: `PASS_PRINCIPLE_LEVEL`.

The construction contract does prioritize minimal degrees and minimal dynamics, so a no-extra-curvature-coupling proposal is independently motivatable as a candidate principle.

C2 physical degrees fixed: `FAIL`.

Current RCG-002 does not contain the closed flat-space local source action/field ontology to which 'minimal covariantization' could be applied. The operational seed has relational two-level probes; G97 has mechanical momentum/force bookkeeping; neither specifies complete local probe+apparatus/support fields.

C3 G97 closed preparation recovery: `DEFINED_TARGET_ONLY`.

MGC can demand G97 recovery, but no actual action is available to prove it.

C4 curvature coupling closure: `PASS_ONLY_IN_AN_UNFIXED_FIELD_BASIS`.

The rule sets explicit curvature couplings to zero by definition, but the source fields and admissible field redefinitions are not fixed. Thus 'zero nonminimal coupling' is not yet a basis-independent physical statement inside current authority.

C5 local source derivation: `FAIL_OBJECT_MISSING`.

No complete source action means no Hilbert source can be derived.

C6 carrier self-source closure: `FAIL`.

MGC is a matter/source rule and does not select the nonlinear carrier action/self-source.

C7 conservation/constraint route: `DEFINED_ONLY_IF_ACTION_EXISTS`.

C8 weak-field recovery: `TARGET_ONLY`.

C9 no free connected datum: `PASS_PRINCIPLE_LEVEL`.

C10 reparametrization robustness: `FAIL_UNFIXED_BASIS`.

### Verdict

`REJECT_INCOMPLETE_SOURCE_ONTOLOGY_AND_BASIS_DEPENDENCE`.

Scientific meaning: 'minimal coupling' is not available as a convention that closes CSA1. It is at best a new principle requiring a prospectively fixed source ontology and field basis.

## P2 — GML

### Assessment

C1 RCG002 grounding: `PASS_STRONG`.

GML is grounded in the only explicit closed preparation object, G97.

C2 physical degrees fixed: `FAIL_LOCAL_ONTOLOGY_INCOMPLETE`.

The validated G97 implementation contains two probe masses/velocities, one apparatus collective mass/velocity, abstract forces `F1,F2,F12`, and impulses `I1,I2`. This is sufficient for exact total-momentum bookkeeping but does not specify a local support continuum, binding/control stress, covariant interaction mechanism, branch internal degrees, or spacetime source density.

C3 G97 closed preparation recovery: `PASS_TARGET_EXACT`.

Any acceptable lift is required to reduce to the exact G97 force/impulse identities.

C4 curvature coupling closure: `FAIL`.

The G97 mechanical equations are flat/mechanical and cannot distinguish covariant lifts that differ only by curvature-dependent operators vanishing in that limit. CSA1 supplies the explicit calibration mechanism showing how such ambiguity can survive identical flat dynamics.

C5 local source derivation: `FAIL_NONUNIQUE_LIFT`.

Abstract force laws do not uniquely determine a local covariant action/stress. Different potentials, local mediator descriptions, support models, or nonlocal interaction functionals can reproduce the same collective force bookkeeping while carrying different local stress.

C6 carrier self-source closure: `FAIL`.

G97 contains no carrier action or self-source law.

C7 conservation/constraint route: `FAIL_LOCAL_ROUTE_MISSING`.

Global momentum closure is not a local covariant constraint-propagation theorem.

C8 weak-field recovery: `DEFINED_TARGET`.

C9 no free connected datum: `PASS_AT_G97_LEVEL`.

C10 reparametrization robustness: `FAIL_LIFT_NONUNIQUENESS`.

### Verdict

`REJECT_MECHANICAL_BOOKKEEPING_DOES_NOT_FIX_LOCAL_COVARIANT_SOURCE`.

## P3 — OSQ

### Assessment

C1 RCG002 grounding: `PASS_STRONG`.

The seed and construction programme are operational, so an operational equivalence principle is naturally motivated.

C2 physical degrees fixed: `FAIL`.

OSQ intentionally tries to quotient source descriptions without first specifying the physical source ontology.

C3 G97 closed preparation recovery: `PASS_AS_DOMAIN_RESTRICTION`.

C4 curvature coupling closure: `FAIL`.

Already-validated weak-field observables need not resolve all curvature-dependent source directions. A coefficient invisible on the validated domain would remain unfixed.

C5 local source derivation: `FAIL_CIRCULAR_FOR_NONLINEAR_USE`.

To quotient two source representatives by equality of the complete carrier/readout response, one needs the source-to-carrier-to-operational map. That is exactly the missing nonlinear map. Restricting the quotient to the existing linearized map would establish only linearized operational equivalence and cannot justify nonlinear RSC source equivalence.

SSE1/CSA1 additionally show that conserved improvements can change the inherited linearized source-carrier coupling, so charge equality is not an operational quotient by itself.

C6 carrier self-source closure: `FAIL`.

C7 conservation/constraint route: `FAIL`.

C8 weak-field recovery: `PASS_BY_CONSTRUCTION_ONLY`.

C9 no free connected datum: `PASS_FORMALLY`.

C10 reparametrization robustness: `DEFINED_OPERATIONALLY_BUT_CIRCULAR`.

### Verdict

`REJECT_CIRCULAR_SOURCE_QUOTIENT_WITHOUT_PHYSICAL_NONLINEAR_MAP`.

## P4 — UVC

### Assessment

C1 RCG002 grounding: `PASS_STRONG`.

UVC directly reflects RSC's desired closed source+carrier self-coupling package.

C2 physical degrees fixed: `FAIL`.

'One action' does not specify the matter/apparatus/support ontology.

C3 G97 closed preparation recovery: `DEFINED_TARGET`.

C4 curvature coupling closure: `FAIL`.

The CSA1 calibration already demonstrates that local covariant actions with the same flat matter dynamics can differ by dimensionless curvature couplings. Requiring one local action, no new dimensionful scale, and two-derivative classical equations does not fix such dimensionless coefficients.

C5 local source derivation: `FAIL_UNDERDEFINED_ACTION`.

C6 carrier self-source closure: `FAIL_UNDERDEFINED_ACTION`.

UVC says carrier and source must come from one action but does not specify the carrier action itself. Importing a standard external gravitational action is forbidden as a selector.

C7 conservation/constraint route: `DEFINED_IF_A_DIFFEOMORPHISM_CONSTITUTION_IS_GIVEN`, but that constitution is not currently supplied.

C8 weak-field recovery: `DEFINED_TARGET`.

C9 no free connected datum: `PASS_PRINCIPLE_LEVEL`.

C10 reparametrization robustness: `FAIL_UNSPECIFIED_EQUIVALENCE_CLASS`.

### Verdict

`REJECT_ONE_ACTION_REQUIREMENT_DOES_NOT_SELECT_THE_ACTION`.

## Frozen controls

Positive structural control behaves correctly: a genuinely complete closed action with all physical fields/couplings fixed, derived total source, carrier self-source, constraint route and baseline recovery would satisfy the structural form of the gate. The current repository simply does not contain such an object.

Negative control A is active: P4 and any naive version of P1 would fail uniqueness if they allow the CSA1 `xi` family without an independent fixing rule.

Negative control B is active: probe-only source bookkeeping remains excluded by G97.

Negative control C is active: no proposal is promoted merely by saying 'simplest' or 'minimal' without a physical field/basis definition.

Negative control D is active: OSQ is not permitted to define its source quotient using the missing nonlinear influence map.

## Aggregate result

Mandatory survivor count: **0**.

Therefore the frozen PASS and ambiguity classifications are not met.

Final classification:

`NO_PREOUTCOME_SOURCE_CONSTITUTION_SELECTED`.

Secondary structural result:

`RSC_SOURCE_SIDE_REQUIRES_NEW_MODEL_DEFINING_ONTOLOGY_SCOPED`.

This is not a theorem that no source constitution can exist. It is a terminal result for the frozen proposal set and current repository authority.

## New scientific fact

The source-side blocker cannot be removed by any of the four most direct current-authority continuations:

- minimal coupling without physical fields;
- direct covariant lifting of G97 mechanics;
- an operational quotient using only current maps;
- a generic one-action source+carrier closure.

The missing datum is now localized to a genuinely model-defining **microphysical source ontology/constitution** strong enough to specify local degrees and curvature couplings before gravitational connected outcomes.

In particular, G97's successful closure should not be overread: it validates collective momentum bookkeeping for a closed preparation, not the local spacetime constitution of that preparation.

## RSC consequence

RSC remains `NEAR_SURVIVOR_NOT_SELECTED` and cannot yet be promoted to a new candidate version.

Current RCG-002 remains `RCG002_CURRENT_VERSION_NONLINEAR_DYNAMICS_UNDERDETERMINED`.

Physical nonlinear completion space remains `UNDEFINED`.

Physical selector rank remains `UNDEFINED_PHYSICAL_MAP_MISSING`.

Programme readiness remains 66%; theory established remains 0%.

## Claim ceiling

No claim is made that:

- minimal coupling is physically false;
- no covariant source ontology exists;
- all source definitions are nonunique;
- RSC is impossible;
- nonlinear gravity or full Bianchi closure has failed;
- a quantum positive influence law exists or fails;
- any connected `chi_ABC` has been predicted;
- new physics or full quantum gravity has been established.

## Exact next admissible gate

Highest-information next gate: `RSC_QUANTUM_CONSTITUTION_PRINCIPLE_TRIAGE_PREOUTCOME_GATE`.

Reason: the source side is now terminal under current-authority principles unless a genuinely new microphysical ontology is introduced. Before adding such new model-defining structure, audit the orthogonal RSC1 quantum blocker to determine whether current RCG-002/RSC commitments can independently select a normalized positive state/measure/influence constitution or whether a second independent new axiom is also required.

This next gate must remain pre-outcome and may use only abstract closed histories/current weak-field carrier structure; it may not compute or select by `chi_ABC`.

If the quantum triage also has zero survivors, the correct conclusion would be that any RSC descendant requires a prospectively new multi-axiom model package rather than a single missing nonlinear coefficient.
