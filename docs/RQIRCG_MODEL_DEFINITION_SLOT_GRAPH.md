# RQIRCG model-definition slot graph

Status: `POST-VB1 INTEGRATION ARCHITECTURE`.

This graph records model-definition obligations for future successor candidates. It does not modify the historical RCG-002 classification and does not promote successor-branch principles into old RCG-002.

Historical lock:

`RCG002_CURRENT_VERSION_NONLINEAR_DYNAMICS_UNDERDETERMINED`.

## Slot A — CLASSICAL LAW

Historical RCG-002: unresolved nonlinear law; VB1 terminal.

Successor-branch information: RQIRCGSF SF021 independently selected `RHPI_SELECTED_AS_CLASSICAL_GRAVITATIONAL_LAW_PRINCIPLE_SCOPED` in its audited classical local reconstruction domain.

Import status: `SUCCESSOR_ONLY / POST-VB1 NEW INFORMATION`.

Forbidden inference: `RCG-002 implies RHPI`.

## Slot B — SOURCE / PREPARATION CONSTITUTION

Parent authority retains G97 closed preparation bookkeeping and the later source-constitution blockers. A predictive successor must specify the same-realization source/preparation law, including apparatus/support/control content required for the actual observable protocol.

This slot is not fixed by classical-law selection alone.

Status in historical parent: `UNRESOLVED_MODEL_DEFINITION_SLOT`.

## Slot C — QUANTUM STATE / MEASURE / BOUNDARY

QCPT1/MAPF1 already establish that microscopic dynamics or positivity alone does not select the physical unobserved state/measure/boundary constitution.

Status: separate model-definition slot.

Critical lock:

`STATE_MEASURE_SELECTION != QUANTUM_LAW_MATCHING_SELECTION`.

SF025 fixed incoming preparation/state while retaining quantum-law matching freedom; therefore a later law ambiguity cannot be renamed an initial-state ambiguity.

## Slot D — QUANTUM LAW / ON-SHELL MATCHING

Newly explicit post-import slot from SF025.

Imported structural negative control:

`POST_RHPI_QUANTUM_COMPOSITION_DOES_NOT_FIX_FINITE_ON_SHELL_MATCHING_SCOPED`.

In the exhibited formal low-energy class, fixing the classical law and incoming preparation and imposing covariance, causal composition, perturbative unitarity and Ward consistency still leaves at least one physically nonredundant positive-loop on-shell matching direction.

Therefore a future quantum selector must have **nonzero selection power on this physical quantum-law fibre**. Generic quantum consistency conditions that merely preserve the SF025 witness direction are not a complete selector.

The SF025 curvature-cubic term is only a witness/counterexample and is not candidate dynamics.

## Slot E — OPERATIONAL OBSERVABLE / READOUT

Newly strengthened by SF026.

The parent retains the exact pairwise-factorized-history theorem:

`Delta_A Delta_B Delta_C W2 = 0`

in its actual independently prescribed-history scope.

Imported scope boundary:

`PAIRWISE_NULL_NOT_STABLE_UNDER_SELF_CONSISTENT_HISTORY_PULLBACK_SCOPED`.

Solving even pairwise Newtonian source dynamics can produce connected three-label dependence in an open on-shell action without a new three-body gravitational vertex.

Therefore the physical observable parent object must be boundary-complete:

`PREPARATION`
`-> FULL FINITE-TIME EVOLUTION`
`-> APPARATUS RECOIL / CONTROL`
`-> RECOMBINATION`
`-> READOUT / POVM`
`-> PHYSICAL PROBABILITY OR COHERENCE`.

An open propagation action, coordinate potential or instantaneous phase-rate kernel is not automatically a measured observable.

## Dependencies and non-collapse rules

The five slots are logically distinct even when one mathematical package relates them:

`CLASSICAL LAW`
`-> constrains SOURCE/PREPARATION compatibility`
`-> participates in finite-time evolution`

while

`QUANTUM STATE/MEASURE`
and
`QUANTUM LAW/MATCHING`

remain separate selection problems, and all four feed the

`OPERATIONAL OBSERVABLE/READOUT`

slot.

No PASS in one slot grants automatic PASS in another.

In particular:

- RHPI classical-law selection does not fix source constitutive coefficients;
- source/preparation closure does not fix a quantum matching law;
- state selection does not fix finite quantum matching coefficients;
- a quantum law does not define a physical probability until preparation, apparatus, boundary and readout are specified;
- a nonzero connected open action does not prove a new interaction vertex.

## Connected-baseline firewall

`NONZERO_CONNECTED_SIGNAL != NEW_THREE_BODY_GRAVITATIONAL_VERTEX`.

Before any future connected effect is interpreted, the same physical protocol must be propagated through the complete authorized lower-order baseline:

1. evolving self-consistent Newtonian pairwise baseline;
2. authorized 1PN / known-physics baseline;
3. then the prospectively selected successor contribution.

The apparatus, preparation, endpoint/boundary data and measurement must be the same across the comparison.

## Historical and successor layers

Historical layer:

`RCG-002` remains frozen at its VB1 nonlinear model-definition boundary.

Successor incubator:

`RQIRCGSF` remains independent and may generate prospective new-principle results.

Integration layer:

The parent imports validated structural obligations/no-go controls from RQIRCGSF but does not retroactively import selected principles as old-model authority.

## Promotion condition

A successor may enter the parent candidate lineage only via a future:

`SUCCESSOR_PROMOTION_GATE`.

Minimum explicit compatible slots:

- `CLASSICAL LAW`;
- `SOURCE CONSTITUTION`;
- `QUANTUM LAW`;
- `STATE / MEASURE`;
- `OPERATIONAL MAP`.

This does not require a full-QG claim, but hidden model-definition slots or outcome-fitted content are disallowed.