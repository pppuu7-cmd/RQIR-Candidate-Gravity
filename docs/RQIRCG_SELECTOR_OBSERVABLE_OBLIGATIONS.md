# RQIRCG selector and observable obligations after RQIRCGSF import

Status: `POST-VB1 STRUCTURAL CONSTRAINT LAYER`.

These obligations apply prospectively to future successor candidates. They do not reopen historical RCG-002 and do not select a new principle in this integration.

## Quantum-law selector obligation — SF025 negative control

Imported classification:

`POST_RHPI_QUANTUM_COMPOSITION_DOES_NOT_FIX_FINITE_ON_SHELL_MATCHING_SCOPED`.

A future quantum-side principle that claims model-definition completeness must constrain the **physical quantum-law / on-shell-matching fibre** with nonzero information rank after classical law, incoming preparation/state, calibration and common physical equivalences are fixed.

It is insufficient to repeat only:

- covariance;
- causal composition/factorization;
- perturbative unitarity;
- Ward identities;
- a fixed classical limit;
- a fixed preparation.

If the proposal leaves the SF025 positive-loop physical matching witness direction admissible in its declared scope, it is not a complete quantum-law selector for that scope.

The SF025 `Riemann^3` finite local direction is a counterexample/witness only. No coefficient or higher-curvature term is imported as RQIRCG dynamics.

## State-versus-law firewall

`QUANTUM_STATE_MEASURE_SELECTION != QUANTUM_LAW_ON_SHELL_MATCHING_SELECTION`.

A matching-law freedom that survives with the same incoming state and preparation may not be reclassified as `UNKNOWN_INITIAL_STATE`.

Conversely, fixing a matching coefficient does not by itself fix the physical state/measure/boundary prescription.

## Proposed-axiom audit checklist

Every future model-defining axiom must be checked for:

1. exact target slot;
2. actual selection power on that slot rather than admissibility only;
3. SF025 negative-control survival when quantum-law completeness is claimed;
4. whether the proposed selection is merely a state/preparation choice;
5. absolute pre-outcome independence from `chi_ABC` and connected outcome values;
6. compatibility with the SF026 self-consistent lower-order connected baseline;
7. whether an open action, coordinate potential or instantaneous diagnostic is being substituted for a physical readout observable.

## Pairwise-null scope and SF026 pullback firewall

Retain the exact factorized-history theorem:

`Delta_A Delta_B Delta_C W2 = 0`

for independently prescribed affine/factorized histories and a linear-plus-quadratic/pairwise functional in the frozen parent scope.

Do **not** promote this theorem to self-consistent trajectories without proof.

Imported classification:

`PAIRWISE_NULL_NOT_STABLE_UNDER_SELF_CONSISTENT_HISTORY_PULLBACK_SCOPED`.

For Newtonian release with

`S_release(q0,T) = -V0*T + (1/3) A0*T^3 + O(T^5)`,

`A0 = (grad V)^T M^-1 (grad V)`,

the frozen SF026 geometry has

`Delta3 S_release = -(82/1848015) (G^2 m^3/ell^4) T^3 + O(T^5)`.

This is a classical open-action baseline diagnostic, not `chi_ABC` and not evidence for a three-body gravitational vertex.

The return-endpoint control has a different coefficient because it is a different boundary-value problem. Boundary protocol may not be chosen after inspecting a desired connected signal.

## Connected-signal interpretation firewall

`NONZERO_CONNECTED_SIGNAL != NEW_THREE_BODY_GRAVITATIONAL_VERTEX`.

A future nonzero `chi_ABC`, probability difference or connected coherence may be attributed to successor physics only after the same full protocol has been evaluated under the complete authorized lower-order baseline.

Minimum comparison ladder:

1. evolving self-consistent Newtonian pairwise baseline;
2. authorized 1PN / known-physics baseline;
3. prospectively selected successor contribution.

The following must be held common across the comparison unless a separately frozen calibration explicitly changes them:

- apparatus;
- preparation maps;
- initial state and source data;
- boundary/endpoint prescription;
- recombination/control sequence;
- readout/POVM;
- finite-size and uncertainty model.

## Boundary-complete observable contract

A physical future prediction requires at least:

`PREPARATION`
`-> FULL FINITE-TIME EVOLUTION`
`-> APPARATUS RECOIL / CONTROL`
`-> RECOMBINATION`
`-> READOUT / POVM`
`-> PHYSICAL PROBABILITY OR COHERENCE`.

A coordinate potential, isolated Hamiltonian term, open propagation action or instantaneous phase rate does not alone satisfy this contract.

Branch-phase tables may be used only when the derived channel actually supports the corresponding coherent approximately diagonal representation. Persistent which-branch records or visibility changes require the fuller state/channel description.

## chi_ABC embargo

This integration does not authorize a new quantum `chi_ABC` calculation in historical RCG-002/RSC.

Imported SF026 diagnostics alter the required baseline interpretation; they do not remove the pre-outcome model-selection embargo.

## Claim ceiling

These obligations do not select RHPI, a quantum law, a source constitution, a state, a matching coefficient or an apparatus protocol for the historical RCG-002 version. They are requirements and negative controls for future successor construction and later promotion.