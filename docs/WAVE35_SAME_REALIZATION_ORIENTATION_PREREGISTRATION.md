# Wave 35 — Same-Realization Orientation Object Availability / Recompute Contract

**Status:** PREREGISTERED BEFORE COMPUTE  
**Physical target:** J8/J9 orientation object for the F1 best pure-gravity fluctuation truncation.

## Question

Is there a reusable public same-realization stability matrix/right-eigenvector basis in the audited F1→later fluctuation literature chain? If not, what exact fresh computation must be produced so that the already validated Wave-33 machinery can generate physical displaced trajectories and a 6x3 UV→RQIR Jacobian with uncertainty?

## Frozen evidence boundary

Wave 35 does not claim that no private/unpublished matrix exists. It asks only whether one is present in the frozen public evidence set and whether later objects can be used without splicing different truncations.

## Predeclared tests

### W35-1 Availability evidence gate
Confirm that the frozen source table credits F1 with fixed point/exponents, later works with their actual outputs, and does not credit an unobserved matrix/eigenbasis.

### W35-2 Same-lineage coverage graph
Represent the required chain:
`UV fixed point -> stability orientation -> 7 displaced RG trajectories -> six same-realization RQIR outputs -> J8 -> J9`.
Score which nodes each frozen source actually supplies.

### W35-3 Anti-splice gate
Show that combining an F1 critical spectrum with a later k=0 vertex/effective-action object from a different truncation does not itself define a same-realization Jacobian.

### W35-4 Orientation sufficiency
Numerically confirm that either a numerical 5x5 stability matrix or a normalized 5x3 real relevant basis is sufficient input for Wave-33 trajectory generation, while eigenvalues alone are not.

### W35-5 Fresh recompute contract
Require a fresh calculation to save:
1. fixed coordinate convention;
2. fixed point;
3. explicit 5x5 approximate stability matrix for each declared closure/variant;
4. normalized right eigenvectors or equivalent real projector;
5. beta-derivative provenance;
6. regulator/truncation/closure metadata;
7. seven symmetric trajectories per orientation object;
8. six RQIR outputs per trajectory;
9. output covariance/uncertainty variants.

### W35-6 Compute budget accounting
For one central orientation object require 7 full trajectory integrations. With `V` additional uncertainty variants, require `7*(1+V)` integrations if each variant is propagated fully. Record one-sided and symmetric alternatives but keep symmetric as authority.

### W35-7 J8/J9 shape contract
Confirm `J8: 6x3` and require a covariance object capable of propagating orientation + trajectory + output uncertainty to J9.

### W35-8 Comparator/provenance firewall
Later physical k=0 form factors/vertices may inform future output projection only if reproduced on the same displaced trajectories. They may not be spliced into the UV orientation object merely because they share authors/program lineage.

### W35-9 Information firewall
No KMQGB/polygon-QGR model information is admitted.

## Pass interpretation

A clean Wave 35 may establish only one of:

- `PUBLIC_ORIENTATION_OBJECT_FOUND_AND_INGESTIBLE`; or
- `PUBLIC_ORIENTATION_OBJECT_NOT_IDENTIFIED_FRESH_RECOMPUTE_CONTRACT_READY`.

The second state is a useful result and does not lower scientific status of the underlying asymptotic-safety program.

## Physical promotion rule

Physical J8 can change from `BLOCKED` only after an actual same-realization orientation object is ingested and the seven displaced trajectories are computed. A metadata audit alone cannot promote J8.
